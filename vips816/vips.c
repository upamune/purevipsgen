//go:build ignore

// Code generated by github.com/upamune/purevipsgen from libvips 8.17.0; DO NOT EDIT.

#include "vips.h"
#include <unistd.h>

// Prerequisites to build, get outputs and cleanup a vips operation

int purevipsgen_operation_execute(VipsOperation *operation, ...) {
    va_list ap;
    if (vips_cache_operation_buildp(&operation)) {
        vips_object_unref_outputs(VIPS_OBJECT(operation));
        g_object_unref(operation);
        return 1;
    }
    va_start(ap, operation);
    const char *name;
    while ((name = va_arg(ap, const char *)) != NULL) {
        void *value = va_arg(ap, void *);
        if (value != NULL) {
            g_object_get(VIPS_OBJECT(operation), name, value, NULL);
        }
    }
    va_end(ap);
    vips_object_unref_outputs(VIPS_OBJECT(operation));
    g_object_unref(operation);
    return 0;
}

int purevipsgen_operation_save_buffer(VipsOperation *operation, void** buf, size_t* len) {
    if (vips_cache_operation_buildp(&operation)) {
        vips_object_unref_outputs(VIPS_OBJECT(operation));
        g_object_unref(operation);
        return 1;
    }
    VipsBlob *blob;
    g_object_get(VIPS_OBJECT(operation), "buffer", &blob, NULL);
    VipsArea *area = VIPS_AREA(blob);
    *buf = (char *)(area->data);
    *len = area->length;
    area->free_fn = NULL;
    vips_area_unref(area);
    vips_object_unref_outputs(VIPS_OBJECT(operation));
    g_object_unref(operation);
    return 0;
}

int purevipsgen_set_int(VipsOperation *operation, const char *name, int value) {
    if (value != 0) { return vips_object_set(VIPS_OBJECT(operation), name, value, NULL); }
    return 0;
}

// purevipsgen_set_keep passes the keep flag to libvips.
// Go Keep(0) is the zero value meaning "not set" — skip and let libvips use its default.
// Go KeepNone (-1) maps to C VIPS_FOREIGN_KEEP_NONE (0), explicitly stripping all metadata.
// All other positive values are bitmask flags and are passed through unchanged.
int purevipsgen_set_keep(VipsOperation *operation, int value) {
    if (value == 0) { return 0; }
    if (value < 0) { value = VIPS_FOREIGN_KEEP_NONE; }
    return vips_object_set(VIPS_OBJECT(operation), "keep", value, NULL);
}

int purevipsgen_set_bool(VipsOperation *operation, const char *name, gboolean value) {
    if (value) { return vips_object_set(VIPS_OBJECT(operation), name, value, NULL); }
    return 0;
}

int purevipsgen_set_double(VipsOperation *operation, const char *name, double value) {
    if (value != 0.0) { return vips_object_set(VIPS_OBJECT(operation), name, value, NULL); }
    return 0;
}

int purevipsgen_set_guint64(VipsOperation *operation, const char *name, guint64 value) {
    if (value != 0) { return vips_object_set(VIPS_OBJECT(operation), name, value, NULL); }
    return 0;
}

int purevipsgen_set_string(VipsOperation *operation, const char *name, const char *value) {
    if (value != NULL && strlen(value) > 0) { return vips_object_set(VIPS_OBJECT(operation), name, value, NULL); }
    return 0;
}

int purevipsgen_set_image(VipsOperation *operation, const char *name, VipsImage *value) {
    if (value != NULL) { return vips_object_set(VIPS_OBJECT(operation), name, value, NULL); }
    return 0;
}

int purevipsgen_set_array_double(VipsOperation *operation, const char *name, VipsArrayDouble *value) {
    if (value != NULL) { return vips_object_set(VIPS_OBJECT(operation), name, value, NULL); }
    return 0;
}

int purevipsgen_set_array_int(VipsOperation *operation, const char *name, VipsArrayInt *value) {
    if (value != NULL) { return vips_object_set(VIPS_OBJECT(operation), name, value, NULL); }
    return 0;
}

int purevipsgen_set_array_image(VipsOperation *operation, const char *name, VipsArrayImage *value) {
    if (value != NULL) { return vips_object_set(VIPS_OBJECT(operation), name, value, NULL); }
    return 0;
}

int purevipsgen_set_interpolate(VipsOperation *operation, const char *name, VipsInterpolate *value) {
    if (value != NULL) { return vips_object_set(VIPS_OBJECT(operation), name, value, NULL); }
    return 0;
}

int purevipsgen_set_source(VipsOperation *operation, const char *name, VipsSource *value) {
    if (value != NULL) { return vips_object_set(VIPS_OBJECT(operation), name, value, NULL); }
    return 0;
}

int purevipsgen_set_target(VipsOperation *operation, const char *name, VipsTarget *value) {
    if (value != NULL) { return vips_object_set(VIPS_OBJECT(operation), name, value, NULL); }
    return 0;
}

// Generated operations

int purevipsgen_CMC2LCh(VipsImage* in, VipsImage** out) {
    return vips_CMC2LCh(in, out, NULL);
}

int purevipsgen_CMYK2XYZ(VipsImage* in, VipsImage** out) {
    return vips_CMYK2XYZ(in, out, NULL);
}

int purevipsgen_HSV2sRGB(VipsImage* in, VipsImage** out) {
    return vips_HSV2sRGB(in, out, NULL);
}

int purevipsgen_LCh2CMC(VipsImage* in, VipsImage** out) {
    return vips_LCh2CMC(in, out, NULL);
}

int purevipsgen_LCh2Lab(VipsImage* in, VipsImage** out) {
    return vips_LCh2Lab(in, out, NULL);
}

int purevipsgen_Lab2LCh(VipsImage* in, VipsImage** out) {
    return vips_Lab2LCh(in, out, NULL);
}

int purevipsgen_Lab2LabQ(VipsImage* in, VipsImage** out) {
    return vips_Lab2LabQ(in, out, NULL);
}

int purevipsgen_Lab2LabS(VipsImage* in, VipsImage** out) {
    return vips_Lab2LabS(in, out, NULL);
}

int purevipsgen_Lab2XYZ(VipsImage* in, VipsImage** out) {
    return vips_Lab2XYZ(in, out, NULL);
}

int purevipsgen_Lab2XYZ_with_options(VipsImage* in, VipsImage** out, double* temp, int temp_n) {
    VipsOperation *operation = vips_operation_new("Lab2XYZ");
    if (!operation) return 1;
    VipsArrayDouble *temp_array = NULL;
    if (temp != NULL && temp_n > 0) { temp_array = vips_array_double_new(temp, temp_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_array_double(operation, "temp", temp_array)
    ) {
        g_object_unref(operation);
        if (temp_array != NULL) { vips_area_unref(VIPS_AREA(temp_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (temp_array != NULL) { vips_area_unref(VIPS_AREA(temp_array)); }
    return result;
}

int purevipsgen_LabQ2Lab(VipsImage* in, VipsImage** out) {
    return vips_LabQ2Lab(in, out, NULL);
}

int purevipsgen_LabQ2LabS(VipsImage* in, VipsImage** out) {
    return vips_LabQ2LabS(in, out, NULL);
}

int purevipsgen_LabQ2sRGB(VipsImage* in, VipsImage** out) {
    return vips_LabQ2sRGB(in, out, NULL);
}

int purevipsgen_LabS2Lab(VipsImage* in, VipsImage** out) {
    return vips_LabS2Lab(in, out, NULL);
}

int purevipsgen_LabS2LabQ(VipsImage* in, VipsImage** out) {
    return vips_LabS2LabQ(in, out, NULL);
}

int purevipsgen_XYZ2CMYK(VipsImage* in, VipsImage** out) {
    return vips_XYZ2CMYK(in, out, NULL);
}

int purevipsgen_XYZ2Lab(VipsImage* in, VipsImage** out) {
    return vips_XYZ2Lab(in, out, NULL);
}

int purevipsgen_XYZ2Lab_with_options(VipsImage* in, VipsImage** out, double* temp, int temp_n) {
    VipsOperation *operation = vips_operation_new("XYZ2Lab");
    if (!operation) return 1;
    VipsArrayDouble *temp_array = NULL;
    if (temp != NULL && temp_n > 0) { temp_array = vips_array_double_new(temp, temp_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_array_double(operation, "temp", temp_array)
    ) {
        g_object_unref(operation);
        if (temp_array != NULL) { vips_area_unref(VIPS_AREA(temp_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (temp_array != NULL) { vips_area_unref(VIPS_AREA(temp_array)); }
    return result;
}

int purevipsgen_XYZ2Yxy(VipsImage* in, VipsImage** out) {
    return vips_XYZ2Yxy(in, out, NULL);
}

int purevipsgen_XYZ2scRGB(VipsImage* in, VipsImage** out) {
    return vips_XYZ2scRGB(in, out, NULL);
}

int purevipsgen_Yxy2XYZ(VipsImage* in, VipsImage** out) {
    return vips_Yxy2XYZ(in, out, NULL);
}

int purevipsgen_abs(VipsImage* in, VipsImage** out) {
    return vips_abs(in, out, NULL);
}

int purevipsgen_add(VipsImage* left, VipsImage* right, VipsImage** out) {
    return vips_add(left, right, out, NULL);
}

int purevipsgen_addalpha(VipsImage* in, VipsImage** out) {
    return vips_addalpha(in, out, NULL);
}

int purevipsgen_affine(VipsImage* in, VipsImage** out, double a, double b, double c, double d) {
    return vips_affine(in, out, a, b, c, d, NULL);
}

int purevipsgen_affine_with_options(VipsImage* in, VipsImage** out, double a, double b, double c, double d, VipsInterpolate* interpolate, int* oarea, int oarea_n, double odx, double ody, double idx, double idy, double* background, int background_n, gboolean premultiplied, VipsExtend extend) {
    VipsOperation *operation = vips_operation_new("affine");
    if (!operation) return 1;
    VipsArrayInt *oarea_array = NULL;
    if (oarea != NULL && oarea_n > 0) { oarea_array = vips_array_int_new(oarea, oarea_n); }
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "a", a, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "b", b, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "c", c, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "d", d, NULL) ||
        purevipsgen_set_interpolate(operation, "interpolate", interpolate) ||
        purevipsgen_set_array_int(operation, "oarea", oarea_array) ||
        purevipsgen_set_double(operation, "odx", odx) ||
        purevipsgen_set_double(operation, "ody", ody) ||
        purevipsgen_set_double(operation, "idx", idx) ||
        purevipsgen_set_double(operation, "idy", idy) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_bool(operation, "premultiplied", premultiplied) ||
        purevipsgen_set_int(operation, "extend", extend)
    ) {
        g_object_unref(operation);
        if (oarea_array != NULL) { vips_area_unref(VIPS_AREA(oarea_array)); }
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (oarea_array != NULL) { vips_area_unref(VIPS_AREA(oarea_array)); }
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_analyzeload(const char* filename, VipsImage** out) {
    return vips_analyzeload(filename, out, NULL);
}

int purevipsgen_analyzeload_with_options(const char* filename, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("analyzeload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_arrayjoin(VipsImage** in, VipsImage** out, int n) {
    return vips_arrayjoin(in, out, n, NULL);
}

int purevipsgen_arrayjoin_with_options(VipsImage** in, VipsImage** out, int n, gint across, gint shim, double* background, int background_n, VipsAlign halign, VipsAlign valign, gint hspacing, gint vspacing) {
    VipsOperation *operation = vips_operation_new("arrayjoin");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in_array, NULL) ||
        purevipsgen_set_int(operation, "across", across) ||
        purevipsgen_set_int(operation, "shim", shim) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "halign", halign) ||
        purevipsgen_set_int(operation, "valign", valign) ||
        purevipsgen_set_int(operation, "hspacing", hspacing) ||
        purevipsgen_set_int(operation, "vspacing", vspacing)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_autorot(VipsImage* in, VipsImage** out) {
    return vips_autorot(in, out, NULL);
}

int purevipsgen_autorot_with_options(VipsImage* in, VipsImage** out, gboolean* flip) {
    VipsOperation *operation = vips_operation_new("autorot");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, "flip", flip, NULL);
    return result;
}

int purevipsgen_avg(VipsImage* in, double* out) {
    return vips_avg(in, out, NULL);
}

int purevipsgen_bandbool(VipsImage* in, VipsImage** out, VipsOperationBoolean boolean) {
    return vips_bandbool(in, out, boolean, NULL);
}

int purevipsgen_bandfold(VipsImage* in, VipsImage** out) {
    return vips_bandfold(in, out, NULL);
}

int purevipsgen_bandfold_with_options(VipsImage* in, VipsImage** out, gint factor) {
    VipsOperation *operation = vips_operation_new("bandfold");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "factor", factor)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_bandjoin(VipsImage** in, VipsImage** out, int n) {
    return vips_bandjoin(in, out, n, NULL);
}

int purevipsgen_bandjoin_const(VipsImage* in, VipsImage** out, double* c, int n) {
    return vips_bandjoin_const(in, out, c, n, NULL);
}

int purevipsgen_bandmean(VipsImage* in, VipsImage** out) {
    return vips_bandmean(in, out, NULL);
}

int purevipsgen_bandrank(VipsImage** in, VipsImage** out, int n) {
    return vips_bandrank(in, out, n, NULL);
}

int purevipsgen_bandrank_with_options(VipsImage** in, VipsImage** out, int n, gint index) {
    VipsOperation *operation = vips_operation_new("bandrank");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in_array, NULL) ||
        purevipsgen_set_int(operation, "index", index)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_bandunfold(VipsImage* in, VipsImage** out) {
    return vips_bandunfold(in, out, NULL);
}

int purevipsgen_bandunfold_with_options(VipsImage* in, VipsImage** out, gint factor) {
    VipsOperation *operation = vips_operation_new("bandunfold");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "factor", factor)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_black(VipsImage** out, gint width, gint height) {
    return vips_black(out, width, height, NULL);
}

int purevipsgen_black_with_options(VipsImage** out, gint width, gint height, gint bands) {
    VipsOperation *operation = vips_operation_new("black");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_int(operation, "bands", bands)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_boolean(VipsImage* left, VipsImage* right, VipsImage** out, VipsOperationBoolean boolean) {
    return vips_boolean(left, right, out, boolean, NULL);
}

int purevipsgen_boolean_const(VipsImage* in, VipsImage** out, VipsOperationBoolean boolean, double* c, int n) {
    return vips_boolean_const(in, out, boolean, c, n, NULL);
}

int purevipsgen_buildlut(VipsImage* in, VipsImage** out) {
    return vips_buildlut(in, out, NULL);
}

int purevipsgen_byteswap(VipsImage* in, VipsImage** out) {
    return vips_byteswap(in, out, NULL);
}

int purevipsgen_canny(VipsImage* in, VipsImage** out) {
    return vips_canny(in, out, NULL);
}

int purevipsgen_canny_with_options(VipsImage* in, VipsImage** out, double sigma, VipsPrecision precision) {
    VipsOperation *operation = vips_operation_new("canny");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_double(operation, "sigma", sigma) ||
        purevipsgen_set_int(operation, "precision", precision)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_case(VipsImage* index, VipsImage** cases, VipsImage** out, int n) {
    return vips_case(index, cases, out, n, NULL);
}

int purevipsgen_cast(VipsImage* in, VipsImage** out, VipsBandFormat format) {
    return vips_cast(in, out, format, NULL);
}

int purevipsgen_cast_with_options(VipsImage* in, VipsImage** out, VipsBandFormat format, gboolean shift) {
    VipsOperation *operation = vips_operation_new("cast");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "format", format, NULL) ||
        purevipsgen_set_bool(operation, "shift", shift)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_clamp(VipsImage* in, VipsImage** out) {
    return vips_clamp(in, out, NULL);
}

int purevipsgen_clamp_with_options(VipsImage* in, VipsImage** out, double min, double max) {
    VipsOperation *operation = vips_operation_new("clamp");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_double(operation, "min", min) ||
        purevipsgen_set_double(operation, "max", max)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_colourspace(VipsImage* in, VipsImage** out, VipsInterpretation space) {
    return vips_colourspace(in, out, space, NULL);
}

int purevipsgen_colourspace_with_options(VipsImage* in, VipsImage** out, VipsInterpretation space, VipsInterpretation source_space) {
    VipsOperation *operation = vips_operation_new("colourspace");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "space", space, NULL) ||
        purevipsgen_set_int(operation, "source_space", source_space)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_compass(VipsImage* in, VipsImage** out, VipsImage* mask) {
    return vips_compass(in, out, mask, NULL);
}

int purevipsgen_compass_with_options(VipsImage* in, VipsImage** out, VipsImage* mask, gint times, VipsAngle45 angle, VipsCombine combine, VipsPrecision precision, gint layers, gint cluster) {
    VipsOperation *operation = vips_operation_new("compass");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "mask", mask, NULL) ||
        purevipsgen_set_int(operation, "times", times) ||
        purevipsgen_set_int(operation, "angle", angle) ||
        purevipsgen_set_int(operation, "combine", combine) ||
        purevipsgen_set_int(operation, "precision", precision) ||
        purevipsgen_set_int(operation, "layers", layers) ||
        purevipsgen_set_int(operation, "cluster", cluster)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_complex(VipsImage* in, VipsImage** out, VipsOperationComplex cmplx) {
    return vips_complex(in, out, cmplx, NULL);
}

int purevipsgen_complex2(VipsImage* left, VipsImage* right, VipsImage** out, VipsOperationComplex2 cmplx) {
    return vips_complex2(left, right, out, cmplx, NULL);
}

int purevipsgen_complexform(VipsImage* left, VipsImage* right, VipsImage** out) {
    return vips_complexform(left, right, out, NULL);
}

int purevipsgen_complexget(VipsImage* in, VipsImage** out, VipsOperationComplexget get) {
    return vips_complexget(in, out, get, NULL);
}

int purevipsgen_composite(VipsImage** in, VipsImage** out, int n, int* mode) {
    return vips_composite(in, out, n, mode, NULL);
}

int purevipsgen_composite_with_options(VipsImage** in, VipsImage** out, int n, int* mode, int* x, int x_n, int* y, int y_n, VipsInterpretation compositing_space, gboolean premultiplied) {
    VipsOperation *operation = vips_operation_new("composite");
    if (!operation) return 1;
    VipsArrayInt *mode_array = NULL;
    if (mode != NULL && n > 1) { mode_array = vips_array_int_new(mode, n-1); }
    VipsArrayInt *x_array = NULL;
    if (x != NULL && x_n > 0) { x_array = vips_array_int_new(x, x_n); }
    VipsArrayInt *y_array = NULL;
    if (y != NULL && y_n > 0) { y_array = vips_array_int_new(y, y_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in_array, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "mode", mode_array, NULL) ||
        purevipsgen_set_array_int(operation, "x", x_array) ||
        purevipsgen_set_array_int(operation, "y", y_array) ||
        purevipsgen_set_int(operation, "compositing_space", compositing_space) ||
        purevipsgen_set_bool(operation, "premultiplied", premultiplied)
    ) {
        g_object_unref(operation);
        if (mode_array != NULL) { vips_area_unref(VIPS_AREA(mode_array)); }
        if (x_array != NULL) { vips_area_unref(VIPS_AREA(x_array)); }
        if (y_array != NULL) { vips_area_unref(VIPS_AREA(y_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (mode_array != NULL) { vips_area_unref(VIPS_AREA(mode_array)); }
    if (x_array != NULL) { vips_area_unref(VIPS_AREA(x_array)); }
    if (y_array != NULL) { vips_area_unref(VIPS_AREA(y_array)); }
    return result;
}

int purevipsgen_composite2(VipsImage* base, VipsImage* overlay, VipsImage** out, VipsBlendMode mode) {
    return vips_composite2(base, overlay, out, mode, NULL);
}

int purevipsgen_composite2_with_options(VipsImage* base, VipsImage* overlay, VipsImage** out, VipsBlendMode mode, gint x, gint y, VipsInterpretation compositing_space, gboolean premultiplied) {
    VipsOperation *operation = vips_operation_new("composite2");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "base", base, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "overlay", overlay, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "mode", mode, NULL) ||
        purevipsgen_set_int(operation, "x", x) ||
        purevipsgen_set_int(operation, "y", y) ||
        purevipsgen_set_int(operation, "compositing_space", compositing_space) ||
        purevipsgen_set_bool(operation, "premultiplied", premultiplied)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_conv(VipsImage* in, VipsImage** out, VipsImage* mask) {
    return vips_conv(in, out, mask, NULL);
}

int purevipsgen_conv_with_options(VipsImage* in, VipsImage** out, VipsImage* mask, VipsPrecision precision, gint layers, gint cluster) {
    VipsOperation *operation = vips_operation_new("conv");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "mask", mask, NULL) ||
        purevipsgen_set_int(operation, "precision", precision) ||
        purevipsgen_set_int(operation, "layers", layers) ||
        purevipsgen_set_int(operation, "cluster", cluster)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_conva(VipsImage* in, VipsImage** out, VipsImage* mask) {
    return vips_conva(in, out, mask, NULL);
}

int purevipsgen_conva_with_options(VipsImage* in, VipsImage** out, VipsImage* mask, gint layers, gint cluster) {
    VipsOperation *operation = vips_operation_new("conva");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "mask", mask, NULL) ||
        purevipsgen_set_int(operation, "layers", layers) ||
        purevipsgen_set_int(operation, "cluster", cluster)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_convasep(VipsImage* in, VipsImage** out, VipsImage* mask) {
    return vips_convasep(in, out, mask, NULL);
}

int purevipsgen_convasep_with_options(VipsImage* in, VipsImage** out, VipsImage* mask, gint layers) {
    VipsOperation *operation = vips_operation_new("convasep");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "mask", mask, NULL) ||
        purevipsgen_set_int(operation, "layers", layers)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_convf(VipsImage* in, VipsImage** out, VipsImage* mask) {
    return vips_convf(in, out, mask, NULL);
}

int purevipsgen_convi(VipsImage* in, VipsImage** out, VipsImage* mask) {
    return vips_convi(in, out, mask, NULL);
}

int purevipsgen_convsep(VipsImage* in, VipsImage** out, VipsImage* mask) {
    return vips_convsep(in, out, mask, NULL);
}

int purevipsgen_convsep_with_options(VipsImage* in, VipsImage** out, VipsImage* mask, VipsPrecision precision, gint layers, gint cluster) {
    VipsOperation *operation = vips_operation_new("convsep");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "mask", mask, NULL) ||
        purevipsgen_set_int(operation, "precision", precision) ||
        purevipsgen_set_int(operation, "layers", layers) ||
        purevipsgen_set_int(operation, "cluster", cluster)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_copy(VipsImage* in, VipsImage** out) {
    return vips_copy(in, out, NULL);
}

int purevipsgen_copy_with_options(VipsImage* in, VipsImage** out, gint width, gint height, gint bands, VipsBandFormat format, VipsCoding coding, VipsInterpretation interpretation, double xres, double yres, gint xoffset, gint yoffset) {
    VipsOperation *operation = vips_operation_new("copy");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "width", width) ||
        purevipsgen_set_int(operation, "height", height) ||
        purevipsgen_set_int(operation, "bands", bands) ||
        purevipsgen_set_int(operation, "format", format) ||
        purevipsgen_set_int(operation, "coding", coding) ||
        purevipsgen_set_int(operation, "interpretation", interpretation) ||
        purevipsgen_set_double(operation, "xres", xres) ||
        purevipsgen_set_double(operation, "yres", yres) ||
        purevipsgen_set_int(operation, "xoffset", xoffset) ||
        purevipsgen_set_int(operation, "yoffset", yoffset)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_countlines(VipsImage* in, double* nolines, VipsDirection direction) {
    return vips_countlines(in, nolines, direction, NULL);
}

int purevipsgen_csvload(const char* filename, VipsImage** out) {
    return vips_csvload(filename, out, NULL);
}

int purevipsgen_csvload_with_options(const char* filename, VipsImage** out, gint skip, gint lines, const char* whitespace, const char* separator, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("csvload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "skip", skip) ||
        purevipsgen_set_int(operation, "lines", lines) ||
        purevipsgen_set_string(operation, "whitespace", whitespace) ||
        purevipsgen_set_string(operation, "separator", separator) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_csvload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_csvload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_csvload_source_with_options(VipsSourceCustom* source, VipsImage** out, gint skip, gint lines, const char* whitespace, const char* separator, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("csvload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_int(operation, "skip", skip) ||
        purevipsgen_set_int(operation, "lines", lines) ||
        purevipsgen_set_string(operation, "whitespace", whitespace) ||
        purevipsgen_set_string(operation, "separator", separator) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_csvsave(VipsImage* in, const char* filename) {
    return vips_csvsave(in, filename, NULL);
}

int purevipsgen_csvsave_with_options(VipsImage* in, const char* filename, const char* separator, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("csvsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_string(operation, "separator", separator) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_csvsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_csvsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_csvsave_target_with_options(VipsImage* in, VipsTargetCustom* target, const char* separator, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("csvsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_string(operation, "separator", separator) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_dE00(VipsImage* left, VipsImage* right, VipsImage** out) {
    return vips_dE00(left, right, out, NULL);
}

int purevipsgen_dE76(VipsImage* left, VipsImage* right, VipsImage** out) {
    return vips_dE76(left, right, out, NULL);
}

int purevipsgen_dECMC(VipsImage* left, VipsImage* right, VipsImage** out) {
    return vips_dECMC(left, right, out, NULL);
}

int purevipsgen_deviate(VipsImage* in, double* out) {
    return vips_deviate(in, out, NULL);
}

int purevipsgen_divide(VipsImage* left, VipsImage* right, VipsImage** out) {
    return vips_divide(left, right, out, NULL);
}

int purevipsgen_draw_circle(VipsImage* image, double* ink, int n, gint cx, gint cy, gint radius) {
    return vips_draw_circle(image, ink, n, cx, cy, radius, NULL);
}

int purevipsgen_draw_circle_with_options(VipsImage* image, double* ink, int n, gint cx, gint cy, gint radius, gboolean fill) {
    VipsOperation *operation = vips_operation_new("draw_circle");
    if (!operation) return 1;
    VipsArrayDouble *ink_array = NULL;
    if (ink != NULL && n > 0) { ink_array = vips_array_double_new(ink, n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "image", image, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "ink", ink_array, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "cx", cx, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "cy", cy, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "radius", radius, NULL) ||
        purevipsgen_set_bool(operation, "fill", fill)
    ) {
        g_object_unref(operation);
        if (ink_array != NULL) { vips_area_unref(VIPS_AREA(ink_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (ink_array != NULL) { vips_area_unref(VIPS_AREA(ink_array)); }
    return result;
}

int purevipsgen_draw_flood(VipsImage* image, double* ink, int n, gint x, gint y) {
    return vips_draw_flood(image, ink, n, x, y, NULL);
}

int purevipsgen_draw_flood_with_options(VipsImage* image, double* ink, int n, gint x, gint y, VipsImage* test, gboolean equal, gint* left, gint* top, gint* width, gint* height) {
    VipsOperation *operation = vips_operation_new("draw_flood");
    if (!operation) return 1;
    VipsArrayDouble *ink_array = NULL;
    if (ink != NULL && n > 0) { ink_array = vips_array_double_new(ink, n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "image", image, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "ink", ink_array, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "x", x, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "y", y, NULL) ||
        purevipsgen_set_image(operation, "test", test) ||
        purevipsgen_set_bool(operation, "equal", equal)
    ) {
        g_object_unref(operation);
        if (ink_array != NULL) { vips_area_unref(VIPS_AREA(ink_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "left", left, "top", top, "width", width, "height", height, NULL);
    if (ink_array != NULL) { vips_area_unref(VIPS_AREA(ink_array)); }
    return result;
}

int purevipsgen_draw_image(VipsImage* image, VipsImage* sub, gint x, gint y) {
    return vips_draw_image(image, sub, x, y, NULL);
}

int purevipsgen_draw_image_with_options(VipsImage* image, VipsImage* sub, gint x, gint y, VipsCombineMode mode) {
    VipsOperation *operation = vips_operation_new("draw_image");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "image", image, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "sub", sub, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "x", x, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "y", y, NULL) ||
        purevipsgen_set_int(operation, "mode", mode)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    return result;
}

int purevipsgen_draw_line(VipsImage* image, double* ink, int n, gint x1, gint y1, gint x2, gint y2) {
    return vips_draw_line(image, ink, n, x1, y1, x2, y2, NULL);
}

int purevipsgen_draw_mask(VipsImage* image, double* ink, int n, VipsImage* mask, gint x, gint y) {
    return vips_draw_mask(image, ink, n, mask, x, y, NULL);
}

int purevipsgen_draw_rect(VipsImage* image, double* ink, int n, gint left, gint top, gint width, gint height) {
    return vips_draw_rect(image, ink, n, left, top, width, height, NULL);
}

int purevipsgen_draw_rect_with_options(VipsImage* image, double* ink, int n, gint left, gint top, gint width, gint height, gboolean fill) {
    VipsOperation *operation = vips_operation_new("draw_rect");
    if (!operation) return 1;
    VipsArrayDouble *ink_array = NULL;
    if (ink != NULL && n > 0) { ink_array = vips_array_double_new(ink, n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "image", image, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "ink", ink_array, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "left", left, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "top", top, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_bool(operation, "fill", fill)
    ) {
        g_object_unref(operation);
        if (ink_array != NULL) { vips_area_unref(VIPS_AREA(ink_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (ink_array != NULL) { vips_area_unref(VIPS_AREA(ink_array)); }
    return result;
}

int purevipsgen_draw_smudge(VipsImage* image, gint left, gint top, gint width, gint height) {
    return vips_draw_smudge(image, left, top, width, height, NULL);
}

int purevipsgen_dzsave(VipsImage* in, const char* filename) {
    return vips_dzsave(in, filename, NULL);
}

int purevipsgen_dzsave_with_options(VipsImage* in, const char* filename, const char* imagename, VipsForeignDzLayout layout, const char* suffix, gint overlap, gint tile_size, gboolean centre, VipsForeignDzDepth depth, VipsAngle angle, VipsForeignDzContainer container, gint compression, VipsRegionShrink region_shrink, gint skip_blanks, const char* id, gint Q, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("dzsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_string(operation, "imagename", imagename) ||
        purevipsgen_set_int(operation, "layout", layout) ||
        purevipsgen_set_string(operation, "suffix", suffix) ||
        purevipsgen_set_int(operation, "overlap", overlap) ||
        purevipsgen_set_int(operation, "tile_size", tile_size) ||
        purevipsgen_set_bool(operation, "centre", centre) ||
        purevipsgen_set_int(operation, "depth", depth) ||
        purevipsgen_set_int(operation, "angle", angle) ||
        purevipsgen_set_int(operation, "container", container) ||
        purevipsgen_set_int(operation, "compression", compression) ||
        purevipsgen_set_int(operation, "region_shrink", region_shrink) ||
        purevipsgen_set_int(operation, "skip_blanks", skip_blanks) ||
        purevipsgen_set_string(operation, "id", id) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_dzsave_buffer(VipsImage* in, void** buf, size_t* len) {
    return vips_dzsave_buffer(in, buf, len, NULL);
}

int purevipsgen_dzsave_buffer_with_options(VipsImage* in, void** buf, size_t* len, const char* imagename, VipsForeignDzLayout layout, const char* suffix, gint overlap, gint tile_size, gboolean centre, VipsForeignDzDepth depth, VipsAngle angle, VipsForeignDzContainer container, gint compression, VipsRegionShrink region_shrink, gint skip_blanks, const char* id, gint Q, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("dzsave_buffer");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_string(operation, "imagename", imagename) ||
        purevipsgen_set_int(operation, "layout", layout) ||
        purevipsgen_set_string(operation, "suffix", suffix) ||
        purevipsgen_set_int(operation, "overlap", overlap) ||
        purevipsgen_set_int(operation, "tile_size", tile_size) ||
        purevipsgen_set_bool(operation, "centre", centre) ||
        purevipsgen_set_int(operation, "depth", depth) ||
        purevipsgen_set_int(operation, "angle", angle) ||
        purevipsgen_set_int(operation, "container", container) ||
        purevipsgen_set_int(operation, "compression", compression) ||
        purevipsgen_set_int(operation, "region_shrink", region_shrink) ||
        purevipsgen_set_int(operation, "skip_blanks", skip_blanks) ||
        purevipsgen_set_string(operation, "id", id) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_save_buffer(operation, buf, len);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_dzsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_dzsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_dzsave_target_with_options(VipsImage* in, VipsTargetCustom* target, const char* imagename, VipsForeignDzLayout layout, const char* suffix, gint overlap, gint tile_size, gboolean centre, VipsForeignDzDepth depth, VipsAngle angle, VipsForeignDzContainer container, gint compression, VipsRegionShrink region_shrink, gint skip_blanks, const char* id, gint Q, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("dzsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_string(operation, "imagename", imagename) ||
        purevipsgen_set_int(operation, "layout", layout) ||
        purevipsgen_set_string(operation, "suffix", suffix) ||
        purevipsgen_set_int(operation, "overlap", overlap) ||
        purevipsgen_set_int(operation, "tile_size", tile_size) ||
        purevipsgen_set_bool(operation, "centre", centre) ||
        purevipsgen_set_int(operation, "depth", depth) ||
        purevipsgen_set_int(operation, "angle", angle) ||
        purevipsgen_set_int(operation, "container", container) ||
        purevipsgen_set_int(operation, "compression", compression) ||
        purevipsgen_set_int(operation, "region_shrink", region_shrink) ||
        purevipsgen_set_int(operation, "skip_blanks", skip_blanks) ||
        purevipsgen_set_string(operation, "id", id) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_embed(VipsImage* in, VipsImage** out, gint x, gint y, gint width, gint height) {
    return vips_embed(in, out, x, y, width, height, NULL);
}

int purevipsgen_embed_with_options(VipsImage* in, VipsImage** out, gint x, gint y, gint width, gint height, VipsExtend extend, double* background, int background_n) {
    VipsOperation *operation = vips_operation_new("embed");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "x", x, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "y", y, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_int(operation, "extend", extend) ||
        purevipsgen_set_array_double(operation, "background", background_array)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_extract_area(VipsImage* input, VipsImage** out, gint left, gint top, gint width, gint height) {
    return vips_extract_area(input, out, left, top, width, height, NULL);
}

int purevipsgen_extract_band(VipsImage* in, VipsImage** out, gint band) {
    return vips_extract_band(in, out, band, NULL);
}

int purevipsgen_extract_band_with_options(VipsImage* in, VipsImage** out, gint band, gint n) {
    VipsOperation *operation = vips_operation_new("extract_band");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "band", band, NULL) ||
        purevipsgen_set_int(operation, "n", n)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_eye(VipsImage** out, gint width, gint height) {
    return vips_eye(out, width, height, NULL);
}

int purevipsgen_eye_with_options(VipsImage** out, gint width, gint height, gboolean uchar, double factor) {
    VipsOperation *operation = vips_operation_new("eye");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_double(operation, "factor", factor)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_falsecolour(VipsImage* in, VipsImage** out) {
    return vips_falsecolour(in, out, NULL);
}

int purevipsgen_fastcor(VipsImage* in, VipsImage* ref, VipsImage** out) {
    return vips_fastcor(in, ref, out, NULL);
}

int purevipsgen_fill_nearest(VipsImage* in, VipsImage** out) {
    return vips_fill_nearest(in, out, NULL);
}

int purevipsgen_find_trim(VipsImage* in, gint* left, gint* top, gint* width, gint* height) {
    return vips_find_trim(in, left, top, width, height, NULL);
}

int purevipsgen_find_trim_with_options(VipsImage* in, gint* left, gint* top, gint* width, gint* height, double threshold, double* background, int background_n, gboolean line_art) {
    VipsOperation *operation = vips_operation_new("find_trim");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_double(operation, "threshold", threshold) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_bool(operation, "line_art", line_art)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "left", left, "top", top, "width", width, "height", height, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_fitsload(const char* filename, VipsImage** out) {
    return vips_fitsload(filename, out, NULL);
}

int purevipsgen_fitsload_with_options(const char* filename, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("fitsload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_fitssave(VipsImage* in, const char* filename) {
    return vips_fitssave(in, filename, NULL);
}

int purevipsgen_fitssave_with_options(VipsImage* in, const char* filename, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("fitssave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_flatten(VipsImage* in, VipsImage** out) {
    return vips_flatten(in, out, NULL);
}

int purevipsgen_flatten_with_options(VipsImage* in, VipsImage** out, double* background, int background_n, double max_alpha) {
    VipsOperation *operation = vips_operation_new("flatten");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_double(operation, "max_alpha", max_alpha)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_flip(VipsImage* in, VipsImage** out, VipsDirection direction) {
    return vips_flip(in, out, direction, NULL);
}

int purevipsgen_float2rad(VipsImage* in, VipsImage** out) {
    return vips_float2rad(in, out, NULL);
}

int purevipsgen_fractsurf(VipsImage** out, gint width, gint height, double fractal_dimension) {
    return vips_fractsurf(out, width, height, fractal_dimension, NULL);
}

int purevipsgen_freqmult(VipsImage* in, VipsImage* mask, VipsImage** out) {
    return vips_freqmult(in, mask, out, NULL);
}

int purevipsgen_fwfft(VipsImage* in, VipsImage** out) {
    return vips_fwfft(in, out, NULL);
}

int purevipsgen_gamma(VipsImage* in, VipsImage** out) {
    return vips_gamma(in, out, NULL);
}

int purevipsgen_gamma_with_options(VipsImage* in, VipsImage** out, double exponent) {
    VipsOperation *operation = vips_operation_new("gamma");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_double(operation, "exponent", exponent)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_gaussblur(VipsImage* in, VipsImage** out, double sigma) {
    return vips_gaussblur(in, out, sigma, NULL);
}

int purevipsgen_gaussblur_with_options(VipsImage* in, VipsImage** out, double sigma, double min_ampl, VipsPrecision precision) {
    VipsOperation *operation = vips_operation_new("gaussblur");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "sigma", sigma, NULL) ||
        purevipsgen_set_double(operation, "min_ampl", min_ampl) ||
        purevipsgen_set_int(operation, "precision", precision)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_gaussmat(VipsImage** out, double sigma, double min_ampl) {
    return vips_gaussmat(out, sigma, min_ampl, NULL);
}

int purevipsgen_gaussmat_with_options(VipsImage** out, double sigma, double min_ampl, gboolean separable, VipsPrecision precision) {
    VipsOperation *operation = vips_operation_new("gaussmat");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "sigma", sigma, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "min_ampl", min_ampl, NULL) ||
        purevipsgen_set_bool(operation, "separable", separable) ||
        purevipsgen_set_int(operation, "precision", precision)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_gaussnoise(VipsImage** out, gint width, gint height) {
    return vips_gaussnoise(out, width, height, NULL);
}

int purevipsgen_gaussnoise_with_options(VipsImage** out, gint width, gint height, double sigma, double mean, gint seed) {
    VipsOperation *operation = vips_operation_new("gaussnoise");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_double(operation, "sigma", sigma) ||
        purevipsgen_set_double(operation, "mean", mean) ||
        purevipsgen_set_int(operation, "seed", seed)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_getpoint(VipsImage* in, double** out_array, int* n, gint x, gint y) {
    return vips_getpoint(in, out_array, n, x, y, NULL);
}

int purevipsgen_getpoint_with_options(VipsImage* in, double** out_array, int* n, gint x, gint y, gboolean unpack_complex) {
    VipsOperation *operation = vips_operation_new("getpoint");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "x", x, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "y", y, NULL) ||
        purevipsgen_set_bool(operation, "unpack_complex", unpack_complex)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out_array", out_array, "n", n, NULL);
    return result;
}

int purevipsgen_gifload(const char* filename, VipsImage** out) {
    return vips_gifload(filename, out, NULL);
}

int purevipsgen_gifload_with_options(const char* filename, VipsImage** out, gint n, gint page, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("gifload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_gifload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_gifload_buffer(buf, len, out, NULL);
}

int purevipsgen_gifload_buffer_with_options(void* buf, size_t len, VipsImage** out, gint n, gint page, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("gifload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_gifload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_gifload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_gifload_source_with_options(VipsSourceCustom* source, VipsImage** out, gint n, gint page, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("gifload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_gifsave(VipsImage* in, const char* filename) {
    return vips_gifsave(in, filename, NULL);
}

int purevipsgen_gifsave_with_options(VipsImage* in, const char* filename, double dither, gint effort, gint bitdepth, double interframe_maxerror, gboolean reuse, double interpalette_maxerror, gboolean interlace, gboolean keep_duplicate_frames, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("gifsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_double(operation, "dither", dither) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_double(operation, "interframe_maxerror", interframe_maxerror) ||
        purevipsgen_set_bool(operation, "reuse", reuse) ||
        purevipsgen_set_double(operation, "interpalette_maxerror", interpalette_maxerror) ||
        purevipsgen_set_bool(operation, "interlace", interlace) ||
        purevipsgen_set_bool(operation, "keep_duplicate_frames", keep_duplicate_frames) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_gifsave_buffer(VipsImage* in, void** buf, size_t* len) {
    return vips_gifsave_buffer(in, buf, len, NULL);
}

int purevipsgen_gifsave_buffer_with_options(VipsImage* in, void** buf, size_t* len, double dither, gint effort, gint bitdepth, double interframe_maxerror, gboolean reuse, double interpalette_maxerror, gboolean interlace, gboolean keep_duplicate_frames, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("gifsave_buffer");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_double(operation, "dither", dither) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_double(operation, "interframe_maxerror", interframe_maxerror) ||
        purevipsgen_set_bool(operation, "reuse", reuse) ||
        purevipsgen_set_double(operation, "interpalette_maxerror", interpalette_maxerror) ||
        purevipsgen_set_bool(operation, "interlace", interlace) ||
        purevipsgen_set_bool(operation, "keep_duplicate_frames", keep_duplicate_frames) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_save_buffer(operation, buf, len);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_gifsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_gifsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_gifsave_target_with_options(VipsImage* in, VipsTargetCustom* target, double dither, gint effort, gint bitdepth, double interframe_maxerror, gboolean reuse, double interpalette_maxerror, gboolean interlace, gboolean keep_duplicate_frames, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("gifsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_double(operation, "dither", dither) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_double(operation, "interframe_maxerror", interframe_maxerror) ||
        purevipsgen_set_bool(operation, "reuse", reuse) ||
        purevipsgen_set_double(operation, "interpalette_maxerror", interpalette_maxerror) ||
        purevipsgen_set_bool(operation, "interlace", interlace) ||
        purevipsgen_set_bool(operation, "keep_duplicate_frames", keep_duplicate_frames) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_globalbalance(VipsImage* in, VipsImage** out) {
    return vips_globalbalance(in, out, NULL);
}

int purevipsgen_globalbalance_with_options(VipsImage* in, VipsImage** out, double gamma, gboolean int_output) {
    VipsOperation *operation = vips_operation_new("globalbalance");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_double(operation, "gamma", gamma) ||
        purevipsgen_set_bool(operation, "int_output", int_output)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_gravity(VipsImage* in, VipsImage** out, VipsCompassDirection direction, gint width, gint height) {
    return vips_gravity(in, out, direction, width, height, NULL);
}

int purevipsgen_gravity_with_options(VipsImage* in, VipsImage** out, VipsCompassDirection direction, gint width, gint height, VipsExtend extend, double* background, int background_n) {
    VipsOperation *operation = vips_operation_new("gravity");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "direction", direction, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_int(operation, "extend", extend) ||
        purevipsgen_set_array_double(operation, "background", background_array)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_grey(VipsImage** out, gint width, gint height) {
    return vips_grey(out, width, height, NULL);
}

int purevipsgen_grey_with_options(VipsImage** out, gint width, gint height, gboolean uchar) {
    VipsOperation *operation = vips_operation_new("grey");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_grid(VipsImage* in, VipsImage** out, gint tile_height, gint across, gint down) {
    return vips_grid(in, out, tile_height, across, down, NULL);
}

int purevipsgen_heifload(const char* filename, VipsImage** out) {
    return vips_heifload(filename, out, NULL);
}

int purevipsgen_heifload_with_options(const char* filename, VipsImage** out, gint page, gint n, gboolean thumbnail, gboolean unlimited, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("heifload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_bool(operation, "thumbnail", thumbnail) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_heifload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_heifload_buffer(buf, len, out, NULL);
}

int purevipsgen_heifload_buffer_with_options(void* buf, size_t len, VipsImage** out, gint page, gint n, gboolean thumbnail, gboolean unlimited, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("heifload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_bool(operation, "thumbnail", thumbnail) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_heifload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_heifload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_heifload_source_with_options(VipsSourceCustom* source, VipsImage** out, gint page, gint n, gboolean thumbnail, gboolean unlimited, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("heifload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_bool(operation, "thumbnail", thumbnail) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_heifsave(VipsImage* in, const char* filename) {
    return vips_heifsave(in, filename, NULL);
}

int purevipsgen_heifsave_with_options(VipsImage* in, const char* filename, gint Q, gint bitdepth, gboolean lossless, VipsForeignHeifCompression compression, gint effort, VipsForeignSubsample subsample_mode, VipsForeignHeifEncoder encoder, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("heifsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "compression", compression) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_int(operation, "subsample_mode", subsample_mode) ||
        purevipsgen_set_int(operation, "encoder", encoder) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_heifsave_buffer(VipsImage* in, void** buf, size_t* len) {
    return vips_heifsave_buffer(in, buf, len, NULL);
}

int purevipsgen_heifsave_buffer_with_options(VipsImage* in, void** buf, size_t* len, gint Q, gint bitdepth, gboolean lossless, VipsForeignHeifCompression compression, gint effort, VipsForeignSubsample subsample_mode, VipsForeignHeifEncoder encoder, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("heifsave_buffer");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "compression", compression) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_int(operation, "subsample_mode", subsample_mode) ||
        purevipsgen_set_int(operation, "encoder", encoder) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_save_buffer(operation, buf, len);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_heifsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_heifsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_heifsave_target_with_options(VipsImage* in, VipsTargetCustom* target, gint Q, gint bitdepth, gboolean lossless, VipsForeignHeifCompression compression, gint effort, VipsForeignSubsample subsample_mode, VipsForeignHeifEncoder encoder, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("heifsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "compression", compression) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_int(operation, "subsample_mode", subsample_mode) ||
        purevipsgen_set_int(operation, "encoder", encoder) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_hist_cum(VipsImage* in, VipsImage** out) {
    return vips_hist_cum(in, out, NULL);
}

int purevipsgen_hist_entropy(VipsImage* in, double* out) {
    return vips_hist_entropy(in, out, NULL);
}

int purevipsgen_hist_equal(VipsImage* in, VipsImage** out) {
    return vips_hist_equal(in, out, NULL);
}

int purevipsgen_hist_equal_with_options(VipsImage* in, VipsImage** out, gint band) {
    VipsOperation *operation = vips_operation_new("hist_equal");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "band", band)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_hist_find(VipsImage* in, VipsImage** out) {
    return vips_hist_find(in, out, NULL);
}

int purevipsgen_hist_find_with_options(VipsImage* in, VipsImage** out, gint band) {
    VipsOperation *operation = vips_operation_new("hist_find");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "band", band)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_hist_find_indexed(VipsImage* in, VipsImage* index, VipsImage** out) {
    return vips_hist_find_indexed(in, index, out, NULL);
}

int purevipsgen_hist_find_indexed_with_options(VipsImage* in, VipsImage* index, VipsImage** out, VipsCombine combine) {
    VipsOperation *operation = vips_operation_new("hist_find_indexed");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "index", index, NULL) ||
        purevipsgen_set_int(operation, "combine", combine)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_hist_find_ndim(VipsImage* in, VipsImage** out) {
    return vips_hist_find_ndim(in, out, NULL);
}

int purevipsgen_hist_find_ndim_with_options(VipsImage* in, VipsImage** out, gint bins) {
    VipsOperation *operation = vips_operation_new("hist_find_ndim");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "bins", bins)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_hist_ismonotonic(VipsImage* in, gboolean* monotonic) {
    return vips_hist_ismonotonic(in, monotonic, NULL);
}

int purevipsgen_hist_local(VipsImage* in, VipsImage** out, gint width, gint height) {
    return vips_hist_local(in, out, width, height, NULL);
}

int purevipsgen_hist_local_with_options(VipsImage* in, VipsImage** out, gint width, gint height, gint max_slope) {
    VipsOperation *operation = vips_operation_new("hist_local");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_int(operation, "max_slope", max_slope)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_hist_match(VipsImage* in, VipsImage* ref, VipsImage** out) {
    return vips_hist_match(in, ref, out, NULL);
}

int purevipsgen_hist_norm(VipsImage* in, VipsImage** out) {
    return vips_hist_norm(in, out, NULL);
}

int purevipsgen_hist_plot(VipsImage* in, VipsImage** out) {
    return vips_hist_plot(in, out, NULL);
}

int purevipsgen_hough_circle(VipsImage* in, VipsImage** out) {
    return vips_hough_circle(in, out, NULL);
}

int purevipsgen_hough_circle_with_options(VipsImage* in, VipsImage** out, gint scale, gint min_radius, gint max_radius) {
    VipsOperation *operation = vips_operation_new("hough_circle");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "scale", scale) ||
        purevipsgen_set_int(operation, "min_radius", min_radius) ||
        purevipsgen_set_int(operation, "max_radius", max_radius)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_hough_line(VipsImage* in, VipsImage** out) {
    return vips_hough_line(in, out, NULL);
}

int purevipsgen_hough_line_with_options(VipsImage* in, VipsImage** out, gint width, gint height) {
    VipsOperation *operation = vips_operation_new("hough_line");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "width", width) ||
        purevipsgen_set_int(operation, "height", height)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_icc_export(VipsImage* in, VipsImage** out) {
    return vips_icc_export(in, out, NULL);
}

int purevipsgen_icc_export_with_options(VipsImage* in, VipsImage** out, VipsPCS pcs, VipsIntent intent, gboolean black_point_compensation, const char* output_profile, gint depth) {
    VipsOperation *operation = vips_operation_new("icc_export");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "pcs", pcs) ||
        purevipsgen_set_int(operation, "intent", intent) ||
        purevipsgen_set_bool(operation, "black_point_compensation", black_point_compensation) ||
        purevipsgen_set_string(operation, "output_profile", output_profile) ||
        purevipsgen_set_int(operation, "depth", depth)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_icc_import(VipsImage* in, VipsImage** out) {
    return vips_icc_import(in, out, NULL);
}

int purevipsgen_icc_import_with_options(VipsImage* in, VipsImage** out, VipsPCS pcs, VipsIntent intent, gboolean black_point_compensation, gboolean embedded, const char* input_profile) {
    VipsOperation *operation = vips_operation_new("icc_import");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "pcs", pcs) ||
        purevipsgen_set_int(operation, "intent", intent) ||
        purevipsgen_set_bool(operation, "black_point_compensation", black_point_compensation) ||
        purevipsgen_set_bool(operation, "embedded", embedded) ||
        purevipsgen_set_string(operation, "input_profile", input_profile)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_icc_transform(VipsImage* in, VipsImage** out, const char* output_profile) {
    return vips_icc_transform(in, out, output_profile, NULL);
}

int purevipsgen_icc_transform_with_options(VipsImage* in, VipsImage** out, const char* output_profile, VipsPCS pcs, VipsIntent intent, gboolean black_point_compensation, gboolean embedded, const char* input_profile, gint depth) {
    VipsOperation *operation = vips_operation_new("icc_transform");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "output_profile", output_profile, NULL) ||
        purevipsgen_set_int(operation, "pcs", pcs) ||
        purevipsgen_set_int(operation, "intent", intent) ||
        purevipsgen_set_bool(operation, "black_point_compensation", black_point_compensation) ||
        purevipsgen_set_bool(operation, "embedded", embedded) ||
        purevipsgen_set_string(operation, "input_profile", input_profile) ||
        purevipsgen_set_int(operation, "depth", depth)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_identity(VipsImage** out) {
    return vips_identity(out, NULL);
}

int purevipsgen_identity_with_options(VipsImage** out, gint bands, gboolean ushort, gint size) {
    VipsOperation *operation = vips_operation_new("identity");
    if (!operation) return 1;
    if (
        purevipsgen_set_int(operation, "bands", bands) ||
        purevipsgen_set_bool(operation, "ushort", ushort) ||
        purevipsgen_set_int(operation, "size", size)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_ifthenelse(VipsImage* cond, VipsImage* in1, VipsImage* in2, VipsImage** out) {
    return vips_ifthenelse(cond, in1, in2, out, NULL);
}

int purevipsgen_ifthenelse_with_options(VipsImage* cond, VipsImage* in1, VipsImage* in2, VipsImage** out, gboolean blend) {
    VipsOperation *operation = vips_operation_new("ifthenelse");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "cond", cond, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "in1", in1, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "in2", in2, NULL) ||
        purevipsgen_set_bool(operation, "blend", blend)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_insert(VipsImage* main, VipsImage* sub, VipsImage** out, gint x, gint y) {
    return vips_insert(main, sub, out, x, y, NULL);
}

int purevipsgen_insert_with_options(VipsImage* main, VipsImage* sub, VipsImage** out, gint x, gint y, gboolean expand, double* background, int background_n) {
    VipsOperation *operation = vips_operation_new("insert");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "main", main, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "sub", sub, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "x", x, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "y", y, NULL) ||
        purevipsgen_set_bool(operation, "expand", expand) ||
        purevipsgen_set_array_double(operation, "background", background_array)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_invert(VipsImage* in, VipsImage** out) {
    return vips_invert(in, out, NULL);
}

int purevipsgen_invertlut(VipsImage* in, VipsImage** out) {
    return vips_invertlut(in, out, NULL);
}

int purevipsgen_invertlut_with_options(VipsImage* in, VipsImage** out, gint size) {
    VipsOperation *operation = vips_operation_new("invertlut");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "size", size)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_invfft(VipsImage* in, VipsImage** out) {
    return vips_invfft(in, out, NULL);
}

int purevipsgen_invfft_with_options(VipsImage* in, VipsImage** out, gboolean real) {
    VipsOperation *operation = vips_operation_new("invfft");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_bool(operation, "real", real)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_join(VipsImage* in1, VipsImage* in2, VipsImage** out, VipsDirection direction) {
    return vips_join(in1, in2, out, direction, NULL);
}

int purevipsgen_join_with_options(VipsImage* in1, VipsImage* in2, VipsImage** out, VipsDirection direction, gboolean expand, gint shim, double* background, int background_n, VipsAlign align) {
    VipsOperation *operation = vips_operation_new("join");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in1", in1, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "in2", in2, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "direction", direction, NULL) ||
        purevipsgen_set_bool(operation, "expand", expand) ||
        purevipsgen_set_int(operation, "shim", shim) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "align", align)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_jp2kload(const char* filename, VipsImage** out) {
    return vips_jp2kload(filename, out, NULL);
}

int purevipsgen_jp2kload_with_options(const char* filename, VipsImage** out, gint page, gboolean oneshot, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("jp2kload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_bool(operation, "oneshot", oneshot) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_jp2kload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_jp2kload_buffer(buf, len, out, NULL);
}

int purevipsgen_jp2kload_buffer_with_options(void* buf, size_t len, VipsImage** out, gint page, gboolean oneshot, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("jp2kload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_bool(operation, "oneshot", oneshot) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_jp2kload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_jp2kload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_jp2kload_source_with_options(VipsSourceCustom* source, VipsImage** out, gint page, gboolean oneshot, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("jp2kload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_bool(operation, "oneshot", oneshot) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_jp2ksave(VipsImage* in, const char* filename) {
    return vips_jp2ksave(in, filename, NULL);
}

int purevipsgen_jp2ksave_with_options(VipsImage* in, const char* filename, gint tile_width, gint tile_height, gboolean lossless, gint Q, VipsForeignSubsample subsample_mode, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("jp2ksave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "tile_width", tile_width) ||
        purevipsgen_set_int(operation, "tile_height", tile_height) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_int(operation, "subsample_mode", subsample_mode) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_jp2ksave_buffer(VipsImage* in, void** buf, size_t* len) {
    return vips_jp2ksave_buffer(in, buf, len, NULL);
}

int purevipsgen_jp2ksave_buffer_with_options(VipsImage* in, void** buf, size_t* len, gint tile_width, gint tile_height, gboolean lossless, gint Q, VipsForeignSubsample subsample_mode, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("jp2ksave_buffer");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "tile_width", tile_width) ||
        purevipsgen_set_int(operation, "tile_height", tile_height) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_int(operation, "subsample_mode", subsample_mode) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_save_buffer(operation, buf, len);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_jp2ksave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_jp2ksave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_jp2ksave_target_with_options(VipsImage* in, VipsTargetCustom* target, gint tile_width, gint tile_height, gboolean lossless, gint Q, VipsForeignSubsample subsample_mode, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("jp2ksave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_int(operation, "tile_width", tile_width) ||
        purevipsgen_set_int(operation, "tile_height", tile_height) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_int(operation, "subsample_mode", subsample_mode) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_jpegload(const char* filename, VipsImage** out) {
    return vips_jpegload(filename, out, NULL);
}

int purevipsgen_jpegload_with_options(const char* filename, VipsImage** out, gint shrink, gboolean autorotate, gboolean unlimited, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("jpegload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "shrink", shrink) ||
        purevipsgen_set_bool(operation, "autorotate", autorotate) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_jpegload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_jpegload_buffer(buf, len, out, NULL);
}

int purevipsgen_jpegload_buffer_with_options(void* buf, size_t len, VipsImage** out, gint shrink, gboolean autorotate, gboolean unlimited, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("jpegload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_int(operation, "shrink", shrink) ||
        purevipsgen_set_bool(operation, "autorotate", autorotate) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_jpegload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_jpegload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_jpegload_source_with_options(VipsSourceCustom* source, VipsImage** out, gint shrink, gboolean autorotate, gboolean unlimited, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("jpegload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_int(operation, "shrink", shrink) ||
        purevipsgen_set_bool(operation, "autorotate", autorotate) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_jpegsave(VipsImage* in, const char* filename) {
    return vips_jpegsave(in, filename, NULL);
}

int purevipsgen_jpegsave_with_options(VipsImage* in, const char* filename, gint Q, gboolean optimize_coding, gboolean interlace, gboolean trellis_quant, gboolean overshoot_deringing, gboolean optimize_scans, gint quant_table, VipsForeignSubsample subsample_mode, gint restart_interval, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("jpegsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_bool(operation, "optimize_coding", optimize_coding) ||
        purevipsgen_set_bool(operation, "interlace", interlace) ||
        purevipsgen_set_bool(operation, "trellis_quant", trellis_quant) ||
        purevipsgen_set_bool(operation, "overshoot_deringing", overshoot_deringing) ||
        purevipsgen_set_bool(operation, "optimize_scans", optimize_scans) ||
        purevipsgen_set_int(operation, "quant_table", quant_table) ||
        purevipsgen_set_int(operation, "subsample_mode", subsample_mode) ||
        purevipsgen_set_int(operation, "restart_interval", restart_interval) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_jpegsave_buffer(VipsImage* in, void** buf, size_t* len) {
    return vips_jpegsave_buffer(in, buf, len, NULL);
}

int purevipsgen_jpegsave_buffer_with_options(VipsImage* in, void** buf, size_t* len, gint Q, gboolean optimize_coding, gboolean interlace, gboolean trellis_quant, gboolean overshoot_deringing, gboolean optimize_scans, gint quant_table, VipsForeignSubsample subsample_mode, gint restart_interval, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("jpegsave_buffer");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_bool(operation, "optimize_coding", optimize_coding) ||
        purevipsgen_set_bool(operation, "interlace", interlace) ||
        purevipsgen_set_bool(operation, "trellis_quant", trellis_quant) ||
        purevipsgen_set_bool(operation, "overshoot_deringing", overshoot_deringing) ||
        purevipsgen_set_bool(operation, "optimize_scans", optimize_scans) ||
        purevipsgen_set_int(operation, "quant_table", quant_table) ||
        purevipsgen_set_int(operation, "subsample_mode", subsample_mode) ||
        purevipsgen_set_int(operation, "restart_interval", restart_interval) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_save_buffer(operation, buf, len);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_jpegsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_jpegsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_jpegsave_target_with_options(VipsImage* in, VipsTargetCustom* target, gint Q, gboolean optimize_coding, gboolean interlace, gboolean trellis_quant, gboolean overshoot_deringing, gboolean optimize_scans, gint quant_table, VipsForeignSubsample subsample_mode, gint restart_interval, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("jpegsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_bool(operation, "optimize_coding", optimize_coding) ||
        purevipsgen_set_bool(operation, "interlace", interlace) ||
        purevipsgen_set_bool(operation, "trellis_quant", trellis_quant) ||
        purevipsgen_set_bool(operation, "overshoot_deringing", overshoot_deringing) ||
        purevipsgen_set_bool(operation, "optimize_scans", optimize_scans) ||
        purevipsgen_set_int(operation, "quant_table", quant_table) ||
        purevipsgen_set_int(operation, "subsample_mode", subsample_mode) ||
        purevipsgen_set_int(operation, "restart_interval", restart_interval) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_jxlload(const char* filename, VipsImage** out) {
    return vips_jxlload(filename, out, NULL);
}

int purevipsgen_jxlload_with_options(const char* filename, VipsImage** out, gint page, gint n, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("jxlload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_jxlload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_jxlload_buffer(buf, len, out, NULL);
}

int purevipsgen_jxlload_buffer_with_options(void* buf, size_t len, VipsImage** out, gint page, gint n, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("jxlload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_jxlload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_jxlload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_jxlload_source_with_options(VipsSourceCustom* source, VipsImage** out, gint page, gint n, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("jxlload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_jxlsave(VipsImage* in, const char* filename) {
    return vips_jxlsave(in, filename, NULL);
}

int purevipsgen_jxlsave_with_options(VipsImage* in, const char* filename, gint tier, double distance, gint effort, gboolean lossless, gint Q, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("jxlsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "tier", tier) ||
        purevipsgen_set_double(operation, "distance", distance) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_jxlsave_buffer(VipsImage* in, void** buf, size_t* len) {
    return vips_jxlsave_buffer(in, buf, len, NULL);
}

int purevipsgen_jxlsave_buffer_with_options(VipsImage* in, void** buf, size_t* len, gint tier, double distance, gint effort, gboolean lossless, gint Q, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("jxlsave_buffer");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "tier", tier) ||
        purevipsgen_set_double(operation, "distance", distance) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_save_buffer(operation, buf, len);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_jxlsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_jxlsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_jxlsave_target_with_options(VipsImage* in, VipsTargetCustom* target, gint tier, double distance, gint effort, gboolean lossless, gint Q, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("jxlsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_int(operation, "tier", tier) ||
        purevipsgen_set_double(operation, "distance", distance) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_labelregions(VipsImage* in, VipsImage** mask) {
    return vips_labelregions(in, mask, NULL);
}

int purevipsgen_labelregions_with_options(VipsImage* in, VipsImage** mask, gint* segments) {
    VipsOperation *operation = vips_operation_new("labelregions");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "mask", mask, "segments", segments, NULL);
    return result;
}

int purevipsgen_linear(VipsImage* in, VipsImage** out, double* a, double* b, int n) {
    return vips_linear(in, out, a, b, n, NULL);
}

int purevipsgen_linear_with_options(VipsImage* in, VipsImage** out, double* a, double* b, int n, gboolean uchar) {
    VipsOperation *operation = vips_operation_new("linear");
    if (!operation) return 1;
    VipsArrayDouble *a_array = NULL;
    if (a != NULL && n > 0) { a_array = vips_array_double_new(a, n); }
    VipsArrayDouble *b_array = NULL;
    if (b != NULL && n > 0) { b_array = vips_array_double_new(b, n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "a", a_array, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "b", b_array, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar)
    ) {
        g_object_unref(operation);
        if (a_array != NULL) { vips_area_unref(VIPS_AREA(a_array)); }
        if (b_array != NULL) { vips_area_unref(VIPS_AREA(b_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (a_array != NULL) { vips_area_unref(VIPS_AREA(a_array)); }
    if (b_array != NULL) { vips_area_unref(VIPS_AREA(b_array)); }
    return result;
}

int purevipsgen_linecache(VipsImage* in, VipsImage** out) {
    return vips_linecache(in, out, NULL);
}

int purevipsgen_linecache_with_options(VipsImage* in, VipsImage** out, gint tile_height, VipsAccess access, gboolean threaded, gboolean persistent) {
    VipsOperation *operation = vips_operation_new("linecache");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "tile_height", tile_height) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_bool(operation, "threaded", threaded) ||
        purevipsgen_set_bool(operation, "persistent", persistent)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_logmat(VipsImage** out, double sigma, double min_ampl) {
    return vips_logmat(out, sigma, min_ampl, NULL);
}

int purevipsgen_logmat_with_options(VipsImage** out, double sigma, double min_ampl, gboolean separable, VipsPrecision precision) {
    VipsOperation *operation = vips_operation_new("logmat");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "sigma", sigma, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "min_ampl", min_ampl, NULL) ||
        purevipsgen_set_bool(operation, "separable", separable) ||
        purevipsgen_set_int(operation, "precision", precision)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_magickload(const char* filename, VipsImage** out) {
    return vips_magickload(filename, out, NULL);
}

int purevipsgen_magickload_with_options(const char* filename, VipsImage** out, const char* density, gint page, gint n, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("magickload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_string(operation, "density", density) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_magickload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_magickload_buffer(buf, len, out, NULL);
}

int purevipsgen_magickload_buffer_with_options(void* buf, size_t len, VipsImage** out, const char* density, gint page, gint n, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("magickload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_string(operation, "density", density) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_magicksave(VipsImage* in, const char* filename) {
    return vips_magicksave(in, filename, NULL);
}

int purevipsgen_magicksave_with_options(VipsImage* in, const char* filename, const char* format, gint quality, gboolean optimize_gif_frames, gboolean optimize_gif_transparency, gint bitdepth, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("magicksave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_string(operation, "format", format) ||
        purevipsgen_set_int(operation, "quality", quality) ||
        purevipsgen_set_bool(operation, "optimize_gif_frames", optimize_gif_frames) ||
        purevipsgen_set_bool(operation, "optimize_gif_transparency", optimize_gif_transparency) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_magicksave_buffer(VipsImage* in, void** buf, size_t* len) {
    return vips_magicksave_buffer(in, buf, len, NULL);
}

int purevipsgen_magicksave_buffer_with_options(VipsImage* in, void** buf, size_t* len, const char* format, gint quality, gboolean optimize_gif_frames, gboolean optimize_gif_transparency, gint bitdepth, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("magicksave_buffer");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_string(operation, "format", format) ||
        purevipsgen_set_int(operation, "quality", quality) ||
        purevipsgen_set_bool(operation, "optimize_gif_frames", optimize_gif_frames) ||
        purevipsgen_set_bool(operation, "optimize_gif_transparency", optimize_gif_transparency) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_save_buffer(operation, buf, len);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_mapim(VipsImage* in, VipsImage** out, VipsImage* index) {
    return vips_mapim(in, out, index, NULL);
}

int purevipsgen_mapim_with_options(VipsImage* in, VipsImage** out, VipsImage* index, VipsInterpolate* interpolate, double* background, int background_n, gboolean premultiplied, VipsExtend extend) {
    VipsOperation *operation = vips_operation_new("mapim");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "index", index, NULL) ||
        purevipsgen_set_interpolate(operation, "interpolate", interpolate) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_bool(operation, "premultiplied", premultiplied) ||
        purevipsgen_set_int(operation, "extend", extend)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_maplut(VipsImage* in, VipsImage** out, VipsImage* lut) {
    return vips_maplut(in, out, lut, NULL);
}

int purevipsgen_maplut_with_options(VipsImage* in, VipsImage** out, VipsImage* lut, gint band) {
    VipsOperation *operation = vips_operation_new("maplut");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "lut", lut, NULL) ||
        purevipsgen_set_int(operation, "band", band)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_mask_butterworth(VipsImage** out, gint width, gint height, double order, double frequency_cutoff, double amplitude_cutoff) {
    return vips_mask_butterworth(out, width, height, order, frequency_cutoff, amplitude_cutoff, NULL);
}

int purevipsgen_mask_butterworth_with_options(VipsImage** out, gint width, gint height, double order, double frequency_cutoff, double amplitude_cutoff, gboolean uchar, gboolean nodc, gboolean reject, gboolean optical) {
    VipsOperation *operation = vips_operation_new("mask_butterworth");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "order", order, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "frequency_cutoff", frequency_cutoff, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "amplitude_cutoff", amplitude_cutoff, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_bool(operation, "nodc", nodc) ||
        purevipsgen_set_bool(operation, "reject", reject) ||
        purevipsgen_set_bool(operation, "optical", optical)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_mask_butterworth_band(VipsImage** out, gint width, gint height, double order, double frequency_cutoff_x, double frequency_cutoff_y, double radius, double amplitude_cutoff) {
    return vips_mask_butterworth_band(out, width, height, order, frequency_cutoff_x, frequency_cutoff_y, radius, amplitude_cutoff, NULL);
}

int purevipsgen_mask_butterworth_band_with_options(VipsImage** out, gint width, gint height, double order, double frequency_cutoff_x, double frequency_cutoff_y, double radius, double amplitude_cutoff, gboolean uchar, gboolean nodc, gboolean reject, gboolean optical) {
    VipsOperation *operation = vips_operation_new("mask_butterworth_band");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "order", order, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "frequency_cutoff_x", frequency_cutoff_x, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "frequency_cutoff_y", frequency_cutoff_y, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "radius", radius, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "amplitude_cutoff", amplitude_cutoff, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_bool(operation, "nodc", nodc) ||
        purevipsgen_set_bool(operation, "reject", reject) ||
        purevipsgen_set_bool(operation, "optical", optical)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_mask_butterworth_ring(VipsImage** out, gint width, gint height, double order, double frequency_cutoff, double amplitude_cutoff, double ringwidth) {
    return vips_mask_butterworth_ring(out, width, height, order, frequency_cutoff, amplitude_cutoff, ringwidth, NULL);
}

int purevipsgen_mask_butterworth_ring_with_options(VipsImage** out, gint width, gint height, double order, double frequency_cutoff, double amplitude_cutoff, double ringwidth, gboolean uchar, gboolean nodc, gboolean reject, gboolean optical) {
    VipsOperation *operation = vips_operation_new("mask_butterworth_ring");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "order", order, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "frequency_cutoff", frequency_cutoff, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "amplitude_cutoff", amplitude_cutoff, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "ringwidth", ringwidth, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_bool(operation, "nodc", nodc) ||
        purevipsgen_set_bool(operation, "reject", reject) ||
        purevipsgen_set_bool(operation, "optical", optical)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_mask_fractal(VipsImage** out, gint width, gint height, double fractal_dimension) {
    return vips_mask_fractal(out, width, height, fractal_dimension, NULL);
}

int purevipsgen_mask_fractal_with_options(VipsImage** out, gint width, gint height, double fractal_dimension, gboolean uchar, gboolean nodc, gboolean reject, gboolean optical) {
    VipsOperation *operation = vips_operation_new("mask_fractal");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "fractal_dimension", fractal_dimension, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_bool(operation, "nodc", nodc) ||
        purevipsgen_set_bool(operation, "reject", reject) ||
        purevipsgen_set_bool(operation, "optical", optical)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_mask_gaussian(VipsImage** out, gint width, gint height, double frequency_cutoff, double amplitude_cutoff) {
    return vips_mask_gaussian(out, width, height, frequency_cutoff, amplitude_cutoff, NULL);
}

int purevipsgen_mask_gaussian_with_options(VipsImage** out, gint width, gint height, double frequency_cutoff, double amplitude_cutoff, gboolean uchar, gboolean nodc, gboolean reject, gboolean optical) {
    VipsOperation *operation = vips_operation_new("mask_gaussian");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "frequency_cutoff", frequency_cutoff, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "amplitude_cutoff", amplitude_cutoff, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_bool(operation, "nodc", nodc) ||
        purevipsgen_set_bool(operation, "reject", reject) ||
        purevipsgen_set_bool(operation, "optical", optical)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_mask_gaussian_band(VipsImage** out, gint width, gint height, double frequency_cutoff_x, double frequency_cutoff_y, double radius, double amplitude_cutoff) {
    return vips_mask_gaussian_band(out, width, height, frequency_cutoff_x, frequency_cutoff_y, radius, amplitude_cutoff, NULL);
}

int purevipsgen_mask_gaussian_band_with_options(VipsImage** out, gint width, gint height, double frequency_cutoff_x, double frequency_cutoff_y, double radius, double amplitude_cutoff, gboolean uchar, gboolean nodc, gboolean reject, gboolean optical) {
    VipsOperation *operation = vips_operation_new("mask_gaussian_band");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "frequency_cutoff_x", frequency_cutoff_x, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "frequency_cutoff_y", frequency_cutoff_y, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "radius", radius, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "amplitude_cutoff", amplitude_cutoff, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_bool(operation, "nodc", nodc) ||
        purevipsgen_set_bool(operation, "reject", reject) ||
        purevipsgen_set_bool(operation, "optical", optical)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_mask_gaussian_ring(VipsImage** out, gint width, gint height, double frequency_cutoff, double amplitude_cutoff, double ringwidth) {
    return vips_mask_gaussian_ring(out, width, height, frequency_cutoff, amplitude_cutoff, ringwidth, NULL);
}

int purevipsgen_mask_gaussian_ring_with_options(VipsImage** out, gint width, gint height, double frequency_cutoff, double amplitude_cutoff, double ringwidth, gboolean uchar, gboolean nodc, gboolean reject, gboolean optical) {
    VipsOperation *operation = vips_operation_new("mask_gaussian_ring");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "frequency_cutoff", frequency_cutoff, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "amplitude_cutoff", amplitude_cutoff, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "ringwidth", ringwidth, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_bool(operation, "nodc", nodc) ||
        purevipsgen_set_bool(operation, "reject", reject) ||
        purevipsgen_set_bool(operation, "optical", optical)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_mask_ideal(VipsImage** out, gint width, gint height, double frequency_cutoff) {
    return vips_mask_ideal(out, width, height, frequency_cutoff, NULL);
}

int purevipsgen_mask_ideal_with_options(VipsImage** out, gint width, gint height, double frequency_cutoff, gboolean uchar, gboolean nodc, gboolean reject, gboolean optical) {
    VipsOperation *operation = vips_operation_new("mask_ideal");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "frequency_cutoff", frequency_cutoff, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_bool(operation, "nodc", nodc) ||
        purevipsgen_set_bool(operation, "reject", reject) ||
        purevipsgen_set_bool(operation, "optical", optical)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_mask_ideal_band(VipsImage** out, gint width, gint height, double frequency_cutoff_x, double frequency_cutoff_y, double radius) {
    return vips_mask_ideal_band(out, width, height, frequency_cutoff_x, frequency_cutoff_y, radius, NULL);
}

int purevipsgen_mask_ideal_band_with_options(VipsImage** out, gint width, gint height, double frequency_cutoff_x, double frequency_cutoff_y, double radius, gboolean uchar, gboolean nodc, gboolean reject, gboolean optical) {
    VipsOperation *operation = vips_operation_new("mask_ideal_band");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "frequency_cutoff_x", frequency_cutoff_x, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "frequency_cutoff_y", frequency_cutoff_y, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "radius", radius, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_bool(operation, "nodc", nodc) ||
        purevipsgen_set_bool(operation, "reject", reject) ||
        purevipsgen_set_bool(operation, "optical", optical)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_mask_ideal_ring(VipsImage** out, gint width, gint height, double frequency_cutoff, double ringwidth) {
    return vips_mask_ideal_ring(out, width, height, frequency_cutoff, ringwidth, NULL);
}

int purevipsgen_mask_ideal_ring_with_options(VipsImage** out, gint width, gint height, double frequency_cutoff, double ringwidth, gboolean uchar, gboolean nodc, gboolean reject, gboolean optical) {
    VipsOperation *operation = vips_operation_new("mask_ideal_ring");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "frequency_cutoff", frequency_cutoff, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "ringwidth", ringwidth, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_bool(operation, "nodc", nodc) ||
        purevipsgen_set_bool(operation, "reject", reject) ||
        purevipsgen_set_bool(operation, "optical", optical)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_match(VipsImage* ref, VipsImage* sec, VipsImage** out, gint xr1, gint yr1, gint xs1, gint ys1, gint xr2, gint yr2, gint xs2, gint ys2) {
    return vips_match(ref, sec, out, xr1, yr1, xs1, ys1, xr2, yr2, xs2, ys2, NULL);
}

int purevipsgen_match_with_options(VipsImage* ref, VipsImage* sec, VipsImage** out, gint xr1, gint yr1, gint xs1, gint ys1, gint xr2, gint yr2, gint xs2, gint ys2, gint hwindow, gint harea, gboolean search, VipsInterpolate* interpolate) {
    VipsOperation *operation = vips_operation_new("match");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "ref", ref, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "sec", sec, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "xr1", xr1, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "yr1", yr1, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "xs1", xs1, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "ys1", ys1, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "xr2", xr2, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "yr2", yr2, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "xs2", xs2, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "ys2", ys2, NULL) ||
        purevipsgen_set_int(operation, "hwindow", hwindow) ||
        purevipsgen_set_int(operation, "harea", harea) ||
        purevipsgen_set_bool(operation, "search", search) ||
        purevipsgen_set_interpolate(operation, "interpolate", interpolate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_math(VipsImage* in, VipsImage** out, VipsOperationMath math) {
    return vips_math(in, out, math, NULL);
}

int purevipsgen_math2(VipsImage* left, VipsImage* right, VipsImage** out, VipsOperationMath2 math2) {
    return vips_math2(left, right, out, math2, NULL);
}

int purevipsgen_math2_const(VipsImage* in, VipsImage** out, VipsOperationMath2 math2, double* c, int n) {
    return vips_math2_const(in, out, math2, c, n, NULL);
}

int purevipsgen_matload(const char* filename, VipsImage** out) {
    return vips_matload(filename, out, NULL);
}

int purevipsgen_matload_with_options(const char* filename, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("matload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_matrixinvert(VipsImage* in, VipsImage** out) {
    return vips_matrixinvert(in, out, NULL);
}

int purevipsgen_matrixload(const char* filename, VipsImage** out) {
    return vips_matrixload(filename, out, NULL);
}

int purevipsgen_matrixload_with_options(const char* filename, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("matrixload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_matrixload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_matrixload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_matrixload_source_with_options(VipsSourceCustom* source, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("matrixload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_matrixmultiply(VipsImage* left, VipsImage* right, VipsImage** out) {
    return vips_matrixmultiply(left, right, out, NULL);
}

int purevipsgen_matrixprint(VipsImage* in) {
    return vips_matrixprint(in, NULL);
}

int purevipsgen_matrixprint_with_options(VipsImage* in, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("matrixprint");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_matrixsave(VipsImage* in, const char* filename) {
    return vips_matrixsave(in, filename, NULL);
}

int purevipsgen_matrixsave_with_options(VipsImage* in, const char* filename, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("matrixsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_matrixsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_matrixsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_matrixsave_target_with_options(VipsImage* in, VipsTargetCustom* target, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("matrixsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_max(VipsImage* in, double* out) {
    return vips_max(in, out, NULL);
}

int purevipsgen_max_with_options(VipsImage* in, double* out, gint size, gint* x, gint* y) {
    VipsOperation *operation = vips_operation_new("max");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "size", size)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, "x", x, "y", y, NULL);
    return result;
}

int purevipsgen_maxpair(VipsImage* left, VipsImage* right, VipsImage** out) {
    return vips_maxpair(left, right, out, NULL);
}

int purevipsgen_measure(VipsImage* in, VipsImage** out, gint h, gint v) {
    return vips_measure(in, out, h, v, NULL);
}

int purevipsgen_measure_with_options(VipsImage* in, VipsImage** out, gint h, gint v, gint left, gint top, gint width, gint height) {
    VipsOperation *operation = vips_operation_new("measure");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "h", h, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "v", v, NULL) ||
        purevipsgen_set_int(operation, "left", left) ||
        purevipsgen_set_int(operation, "top", top) ||
        purevipsgen_set_int(operation, "width", width) ||
        purevipsgen_set_int(operation, "height", height)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_merge(VipsImage* ref, VipsImage* sec, VipsImage** out, VipsDirection direction, gint dx, gint dy) {
    return vips_merge(ref, sec, out, direction, dx, dy, NULL);
}

int purevipsgen_merge_with_options(VipsImage* ref, VipsImage* sec, VipsImage** out, VipsDirection direction, gint dx, gint dy, gint mblend) {
    VipsOperation *operation = vips_operation_new("merge");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "ref", ref, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "sec", sec, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "direction", direction, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "dx", dx, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "dy", dy, NULL) ||
        purevipsgen_set_int(operation, "mblend", mblend)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_min(VipsImage* in, double* out) {
    return vips_min(in, out, NULL);
}

int purevipsgen_min_with_options(VipsImage* in, double* out, gint size, gint* x, gint* y) {
    VipsOperation *operation = vips_operation_new("min");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "size", size)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, "x", x, "y", y, NULL);
    return result;
}

int purevipsgen_minpair(VipsImage* left, VipsImage* right, VipsImage** out) {
    return vips_minpair(left, right, out, NULL);
}

int purevipsgen_morph(VipsImage* in, VipsImage** out, VipsImage* mask, VipsOperationMorphology morph) {
    return vips_morph(in, out, mask, morph, NULL);
}

int purevipsgen_mosaic(VipsImage* ref, VipsImage* sec, VipsImage** out, VipsDirection direction, gint xref, gint yref, gint xsec, gint ysec) {
    return vips_mosaic(ref, sec, out, direction, xref, yref, xsec, ysec, NULL);
}

int purevipsgen_mosaic_with_options(VipsImage* ref, VipsImage* sec, VipsImage** out, VipsDirection direction, gint xref, gint yref, gint xsec, gint ysec, gint hwindow, gint harea, gint mblend, gint bandno, gint* dx0, gint* dy0, double* scale1, double* angle1, double* dy1, double* dx1) {
    VipsOperation *operation = vips_operation_new("mosaic");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "ref", ref, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "sec", sec, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "direction", direction, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "xref", xref, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "yref", yref, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "xsec", xsec, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "ysec", ysec, NULL) ||
        purevipsgen_set_int(operation, "hwindow", hwindow) ||
        purevipsgen_set_int(operation, "harea", harea) ||
        purevipsgen_set_int(operation, "mblend", mblend) ||
        purevipsgen_set_int(operation, "bandno", bandno)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, "dx0", dx0, "dy0", dy0, "scale1", scale1, "angle1", angle1, "dy1", dy1, "dx1", dx1, NULL);
    return result;
}

int purevipsgen_mosaic1(VipsImage* ref, VipsImage* sec, VipsImage** out, VipsDirection direction, gint xr1, gint yr1, gint xs1, gint ys1, gint xr2, gint yr2, gint xs2, gint ys2) {
    return vips_mosaic1(ref, sec, out, direction, xr1, yr1, xs1, ys1, xr2, yr2, xs2, ys2, NULL);
}

int purevipsgen_mosaic1_with_options(VipsImage* ref, VipsImage* sec, VipsImage** out, VipsDirection direction, gint xr1, gint yr1, gint xs1, gint ys1, gint xr2, gint yr2, gint xs2, gint ys2, gint hwindow, gint harea, gboolean search, VipsInterpolate* interpolate, gint mblend) {
    VipsOperation *operation = vips_operation_new("mosaic1");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "ref", ref, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "sec", sec, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "direction", direction, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "xr1", xr1, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "yr1", yr1, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "xs1", xs1, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "ys1", ys1, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "xr2", xr2, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "yr2", yr2, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "xs2", xs2, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "ys2", ys2, NULL) ||
        purevipsgen_set_int(operation, "hwindow", hwindow) ||
        purevipsgen_set_int(operation, "harea", harea) ||
        purevipsgen_set_bool(operation, "search", search) ||
        purevipsgen_set_interpolate(operation, "interpolate", interpolate) ||
        purevipsgen_set_int(operation, "mblend", mblend)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_msb(VipsImage* in, VipsImage** out) {
    return vips_msb(in, out, NULL);
}

int purevipsgen_msb_with_options(VipsImage* in, VipsImage** out, gint band) {
    VipsOperation *operation = vips_operation_new("msb");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "band", band)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_multiply(VipsImage* left, VipsImage* right, VipsImage** out) {
    return vips_multiply(left, right, out, NULL);
}

int purevipsgen_openexrload(const char* filename, VipsImage** out) {
    return vips_openexrload(filename, out, NULL);
}

int purevipsgen_openexrload_with_options(const char* filename, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("openexrload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_openslideload(const char* filename, VipsImage** out) {
    return vips_openslideload(filename, out, NULL);
}

int purevipsgen_openslideload_with_options(const char* filename, VipsImage** out, gint level, gboolean autocrop, const char* associated, gboolean attach_associated, gboolean rgb, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("openslideload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "level", level) ||
        purevipsgen_set_bool(operation, "autocrop", autocrop) ||
        purevipsgen_set_string(operation, "associated", associated) ||
        purevipsgen_set_bool(operation, "attach_associated", attach_associated) ||
        purevipsgen_set_bool(operation, "rgb", rgb) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_openslideload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_openslideload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_openslideload_source_with_options(VipsSourceCustom* source, VipsImage** out, gint level, gboolean autocrop, const char* associated, gboolean attach_associated, gboolean rgb, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("openslideload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_int(operation, "level", level) ||
        purevipsgen_set_bool(operation, "autocrop", autocrop) ||
        purevipsgen_set_string(operation, "associated", associated) ||
        purevipsgen_set_bool(operation, "attach_associated", attach_associated) ||
        purevipsgen_set_bool(operation, "rgb", rgb) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_pdfload(const char* filename, VipsImage** out) {
    return vips_pdfload(filename, out, NULL);
}

int purevipsgen_pdfload_with_options(const char* filename, VipsImage** out, gint page, gint n, double dpi, double scale, double* background, int background_n, const char* password, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("pdfload");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_double(operation, "dpi", dpi) ||
        purevipsgen_set_double(operation, "scale", scale) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_string(operation, "password", password) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_pdfload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_pdfload_buffer(buf, len, out, NULL);
}

int purevipsgen_pdfload_buffer_with_options(void* buf, size_t len, VipsImage** out, gint page, gint n, double dpi, double scale, double* background, int background_n, const char* password, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("pdfload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_double(operation, "dpi", dpi) ||
        purevipsgen_set_double(operation, "scale", scale) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_string(operation, "password", password) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_pdfload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_pdfload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_pdfload_source_with_options(VipsSourceCustom* source, VipsImage** out, gint page, gint n, double dpi, double scale, double* background, int background_n, const char* password, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("pdfload_source");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_double(operation, "dpi", dpi) ||
        purevipsgen_set_double(operation, "scale", scale) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_string(operation, "password", password) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_percent(VipsImage* in, double percent, gint* threshold) {
    return vips_percent(in, percent, threshold, NULL);
}

int purevipsgen_perlin(VipsImage** out, gint width, gint height) {
    return vips_perlin(out, width, height, NULL);
}

int purevipsgen_perlin_with_options(VipsImage** out, gint width, gint height, gint cell_size, gboolean uchar, gint seed) {
    VipsOperation *operation = vips_operation_new("perlin");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_int(operation, "cell_size", cell_size) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_int(operation, "seed", seed)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_phasecor(VipsImage* in, VipsImage* in2, VipsImage** out) {
    return vips_phasecor(in, in2, out, NULL);
}

int purevipsgen_pngload(const char* filename, VipsImage** out) {
    return vips_pngload(filename, out, NULL);
}

int purevipsgen_pngload_with_options(const char* filename, VipsImage** out, gboolean unlimited, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("pngload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_pngload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_pngload_buffer(buf, len, out, NULL);
}

int purevipsgen_pngload_buffer_with_options(void* buf, size_t len, VipsImage** out, gboolean unlimited, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("pngload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_pngload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_pngload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_pngload_source_with_options(VipsSourceCustom* source, VipsImage** out, gboolean unlimited, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("pngload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_pngsave(VipsImage* in, const char* filename) {
    return vips_pngsave(in, filename, NULL);
}

int purevipsgen_pngsave_with_options(VipsImage* in, const char* filename, gint compression, gboolean interlace, VipsForeignPngFilter filter, gboolean palette, gint Q, double dither, gint bitdepth, gint effort, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("pngsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "compression", compression) ||
        purevipsgen_set_bool(operation, "interlace", interlace) ||
        purevipsgen_set_int(operation, "filter", filter) ||
        purevipsgen_set_bool(operation, "palette", palette) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_double(operation, "dither", dither) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_pngsave_buffer(VipsImage* in, void** buf, size_t* len) {
    return vips_pngsave_buffer(in, buf, len, NULL);
}

int purevipsgen_pngsave_buffer_with_options(VipsImage* in, void** buf, size_t* len, gint compression, gboolean interlace, VipsForeignPngFilter filter, gboolean palette, gint Q, double dither, gint bitdepth, gint effort, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("pngsave_buffer");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "compression", compression) ||
        purevipsgen_set_bool(operation, "interlace", interlace) ||
        purevipsgen_set_int(operation, "filter", filter) ||
        purevipsgen_set_bool(operation, "palette", palette) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_double(operation, "dither", dither) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_save_buffer(operation, buf, len);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_pngsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_pngsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_pngsave_target_with_options(VipsImage* in, VipsTargetCustom* target, gint compression, gboolean interlace, VipsForeignPngFilter filter, gboolean palette, gint Q, double dither, gint bitdepth, gint effort, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("pngsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_int(operation, "compression", compression) ||
        purevipsgen_set_bool(operation, "interlace", interlace) ||
        purevipsgen_set_int(operation, "filter", filter) ||
        purevipsgen_set_bool(operation, "palette", palette) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_double(operation, "dither", dither) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_ppmload(const char* filename, VipsImage** out) {
    return vips_ppmload(filename, out, NULL);
}

int purevipsgen_ppmload_with_options(const char* filename, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("ppmload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_ppmload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_ppmload_buffer(buf, len, out, NULL);
}

int purevipsgen_ppmload_buffer_with_options(void* buf, size_t len, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("ppmload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_ppmload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_ppmload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_ppmload_source_with_options(VipsSourceCustom* source, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("ppmload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_ppmsave(VipsImage* in, const char* filename) {
    return vips_ppmsave(in, filename, NULL);
}

int purevipsgen_ppmsave_with_options(VipsImage* in, const char* filename, VipsForeignPpmFormat format, gboolean ascii, gint bitdepth, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("ppmsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "format", format) ||
        purevipsgen_set_bool(operation, "ascii", ascii) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_ppmsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_ppmsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_ppmsave_target_with_options(VipsImage* in, VipsTargetCustom* target, VipsForeignPpmFormat format, gboolean ascii, gint bitdepth, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("ppmsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_int(operation, "format", format) ||
        purevipsgen_set_bool(operation, "ascii", ascii) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_premultiply(VipsImage* in, VipsImage** out) {
    return vips_premultiply(in, out, NULL);
}

int purevipsgen_premultiply_with_options(VipsImage* in, VipsImage** out, double max_alpha) {
    VipsOperation *operation = vips_operation_new("premultiply");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_double(operation, "max_alpha", max_alpha)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_prewitt(VipsImage* in, VipsImage** out) {
    return vips_prewitt(in, out, NULL);
}

int purevipsgen_profile(VipsImage* in, VipsImage** columns, VipsImage** rows) {
    return vips_profile(in, columns, rows, NULL);
}

int purevipsgen_profile_load(const char* name, VipsBlob** profile) {
    return vips_profile_load(name, profile, NULL);
}

int purevipsgen_project(VipsImage* in, VipsImage** columns, VipsImage** rows) {
    return vips_project(in, columns, rows, NULL);
}

int purevipsgen_quadratic(VipsImage* in, VipsImage** out, VipsImage* coeff) {
    return vips_quadratic(in, out, coeff, NULL);
}

int purevipsgen_quadratic_with_options(VipsImage* in, VipsImage** out, VipsImage* coeff, VipsInterpolate* interpolate) {
    VipsOperation *operation = vips_operation_new("quadratic");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "coeff", coeff, NULL) ||
        purevipsgen_set_interpolate(operation, "interpolate", interpolate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_rad2float(VipsImage* in, VipsImage** out) {
    return vips_rad2float(in, out, NULL);
}

int purevipsgen_radload(const char* filename, VipsImage** out) {
    return vips_radload(filename, out, NULL);
}

int purevipsgen_radload_with_options(const char* filename, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("radload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_radload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_radload_buffer(buf, len, out, NULL);
}

int purevipsgen_radload_buffer_with_options(void* buf, size_t len, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("radload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_radload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_radload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_radload_source_with_options(VipsSourceCustom* source, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("radload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_radsave(VipsImage* in, const char* filename) {
    return vips_radsave(in, filename, NULL);
}

int purevipsgen_radsave_with_options(VipsImage* in, const char* filename, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("radsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_radsave_buffer(VipsImage* in, void** buf, size_t* len) {
    return vips_radsave_buffer(in, buf, len, NULL);
}

int purevipsgen_radsave_buffer_with_options(VipsImage* in, void** buf, size_t* len, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("radsave_buffer");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_save_buffer(operation, buf, len);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_radsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_radsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_radsave_target_with_options(VipsImage* in, VipsTargetCustom* target, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("radsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_rank(VipsImage* in, VipsImage** out, gint width, gint height, gint index) {
    return vips_rank(in, out, width, height, index, NULL);
}

int purevipsgen_rawload(const char* filename, VipsImage** out, gint width, gint height, gint bands) {
    return vips_rawload(filename, out, width, height, bands, NULL);
}

int purevipsgen_rawload_with_options(const char* filename, VipsImage** out, gint width, gint height, gint bands, guint64 offset, VipsBandFormat format, VipsInterpretation interpretation, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("rawload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "bands", bands, NULL) ||
        purevipsgen_set_guint64(operation, "offset", offset) ||
        purevipsgen_set_int(operation, "format", format) ||
        purevipsgen_set_int(operation, "interpretation", interpretation) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_rawsave(VipsImage* in, const char* filename) {
    return vips_rawsave(in, filename, NULL);
}

int purevipsgen_rawsave_with_options(VipsImage* in, const char* filename, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("rawsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_rawsave_buffer(VipsImage* in, void** buf, size_t* len) {
    return vips_rawsave_buffer(in, buf, len, NULL);
}

int purevipsgen_rawsave_buffer_with_options(VipsImage* in, void** buf, size_t* len, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("rawsave_buffer");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_save_buffer(operation, buf, len);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_rawsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_rawsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_rawsave_target_with_options(VipsImage* in, VipsTargetCustom* target, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("rawsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_recomb(VipsImage* in, VipsImage** out, VipsImage* m) {
    return vips_recomb(in, out, m, NULL);
}

int purevipsgen_reduce(VipsImage* in, VipsImage** out, double hshrink, double vshrink) {
    return vips_reduce(in, out, hshrink, vshrink, NULL);
}

int purevipsgen_reduce_with_options(VipsImage* in, VipsImage** out, double hshrink, double vshrink, VipsKernel kernel, double gap) {
    VipsOperation *operation = vips_operation_new("reduce");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "hshrink", hshrink, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "vshrink", vshrink, NULL) ||
        purevipsgen_set_int(operation, "kernel", kernel) ||
        purevipsgen_set_double(operation, "gap", gap)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_reduceh(VipsImage* in, VipsImage** out, double hshrink) {
    return vips_reduceh(in, out, hshrink, NULL);
}

int purevipsgen_reduceh_with_options(VipsImage* in, VipsImage** out, double hshrink, VipsKernel kernel, double gap) {
    VipsOperation *operation = vips_operation_new("reduceh");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "hshrink", hshrink, NULL) ||
        purevipsgen_set_int(operation, "kernel", kernel) ||
        purevipsgen_set_double(operation, "gap", gap)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_reducev(VipsImage* in, VipsImage** out, double vshrink) {
    return vips_reducev(in, out, vshrink, NULL);
}

int purevipsgen_reducev_with_options(VipsImage* in, VipsImage** out, double vshrink, VipsKernel kernel, double gap) {
    VipsOperation *operation = vips_operation_new("reducev");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "vshrink", vshrink, NULL) ||
        purevipsgen_set_int(operation, "kernel", kernel) ||
        purevipsgen_set_double(operation, "gap", gap)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_relational(VipsImage* left, VipsImage* right, VipsImage** out, VipsOperationRelational relational) {
    return vips_relational(left, right, out, relational, NULL);
}

int purevipsgen_relational_const(VipsImage* in, VipsImage** out, VipsOperationRelational relational, double* c, int n) {
    return vips_relational_const(in, out, relational, c, n, NULL);
}

int purevipsgen_remainder(VipsImage* left, VipsImage* right, VipsImage** out) {
    return vips_remainder(left, right, out, NULL);
}

int purevipsgen_remainder_const(VipsImage* in, VipsImage** out, double* c, int n) {
    return vips_remainder_const(in, out, c, n, NULL);
}

int purevipsgen_remosaic(VipsImage* in, VipsImage** out, const char* old_str, const char* new_str) {
    return vips_remosaic(in, out, old_str, new_str, NULL);
}

int purevipsgen_replicate(VipsImage* in, VipsImage** out, gint across, gint down) {
    return vips_replicate(in, out, across, down, NULL);
}

int purevipsgen_resize(VipsImage* in, VipsImage** out, double scale) {
    return vips_resize(in, out, scale, NULL);
}

int purevipsgen_resize_with_options(VipsImage* in, VipsImage** out, double scale, VipsKernel kernel, double gap, double vscale) {
    VipsOperation *operation = vips_operation_new("resize");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "scale", scale, NULL) ||
        purevipsgen_set_int(operation, "kernel", kernel) ||
        purevipsgen_set_double(operation, "gap", gap) ||
        purevipsgen_set_double(operation, "vscale", vscale)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_rot(VipsImage* in, VipsImage** out, VipsAngle angle) {
    return vips_rot(in, out, angle, NULL);
}

int purevipsgen_rot45(VipsImage* in, VipsImage** out) {
    return vips_rot45(in, out, NULL);
}

int purevipsgen_rot45_with_options(VipsImage* in, VipsImage** out, VipsAngle45 angle) {
    VipsOperation *operation = vips_operation_new("rot45");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "angle", angle)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_rotate(VipsImage* in, VipsImage** out, double angle) {
    return vips_rotate(in, out, angle, NULL);
}

int purevipsgen_rotate_with_options(VipsImage* in, VipsImage** out, double angle, VipsInterpolate* interpolate, double* background, int background_n, double odx, double ody, double idx, double idy) {
    VipsOperation *operation = vips_operation_new("rotate");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "angle", angle, NULL) ||
        purevipsgen_set_interpolate(operation, "interpolate", interpolate) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_double(operation, "odx", odx) ||
        purevipsgen_set_double(operation, "ody", ody) ||
        purevipsgen_set_double(operation, "idx", idx) ||
        purevipsgen_set_double(operation, "idy", idy)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_round(VipsImage* in, VipsImage** out, VipsOperationRound round) {
    return vips_round(in, out, round, NULL);
}

int purevipsgen_sRGB2HSV(VipsImage* in, VipsImage** out) {
    return vips_sRGB2HSV(in, out, NULL);
}

int purevipsgen_sRGB2scRGB(VipsImage* in, VipsImage** out) {
    return vips_sRGB2scRGB(in, out, NULL);
}

int purevipsgen_scRGB2BW(VipsImage* in, VipsImage** out) {
    return vips_scRGB2BW(in, out, NULL);
}

int purevipsgen_scRGB2BW_with_options(VipsImage* in, VipsImage** out, gint depth) {
    VipsOperation *operation = vips_operation_new("scRGB2BW");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "depth", depth)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_scRGB2XYZ(VipsImage* in, VipsImage** out) {
    return vips_scRGB2XYZ(in, out, NULL);
}

int purevipsgen_scRGB2sRGB(VipsImage* in, VipsImage** out) {
    return vips_scRGB2sRGB(in, out, NULL);
}

int purevipsgen_scRGB2sRGB_with_options(VipsImage* in, VipsImage** out, gint depth) {
    VipsOperation *operation = vips_operation_new("scRGB2sRGB");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "depth", depth)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_scale(VipsImage* in, VipsImage** out) {
    return vips_scale(in, out, NULL);
}

int purevipsgen_scale_with_options(VipsImage* in, VipsImage** out, double exp, gboolean log) {
    VipsOperation *operation = vips_operation_new("scale");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_double(operation, "exp", exp) ||
        purevipsgen_set_bool(operation, "log", log)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_scharr(VipsImage* in, VipsImage** out) {
    return vips_scharr(in, out, NULL);
}

int purevipsgen_sdf(VipsImage** out, gint width, gint height, VipsSdfShape shape) {
    return vips_sdf(out, width, height, shape, NULL);
}

int purevipsgen_sdf_with_options(VipsImage** out, gint width, gint height, VipsSdfShape shape, double r, double* a, int a_n, double* b, int b_n, double* corners, int corners_n) {
    VipsOperation *operation = vips_operation_new("sdf");
    if (!operation) return 1;
    VipsArrayDouble *a_array = NULL;
    if (a != NULL && a_n > 0) { a_array = vips_array_double_new(a, a_n); }
    VipsArrayDouble *b_array = NULL;
    if (b != NULL && b_n > 0) { b_array = vips_array_double_new(b, b_n); }
    VipsArrayDouble *corners_array = NULL;
    if (corners != NULL && corners_n > 0) { corners_array = vips_array_double_new(corners, corners_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "shape", shape, NULL) ||
        purevipsgen_set_double(operation, "r", r) ||
        purevipsgen_set_array_double(operation, "a", a_array) ||
        purevipsgen_set_array_double(operation, "b", b_array) ||
        purevipsgen_set_array_double(operation, "corners", corners_array)
    ) {
        g_object_unref(operation);
        if (a_array != NULL) { vips_area_unref(VIPS_AREA(a_array)); }
        if (b_array != NULL) { vips_area_unref(VIPS_AREA(b_array)); }
        if (corners_array != NULL) { vips_area_unref(VIPS_AREA(corners_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (a_array != NULL) { vips_area_unref(VIPS_AREA(a_array)); }
    if (b_array != NULL) { vips_area_unref(VIPS_AREA(b_array)); }
    if (corners_array != NULL) { vips_area_unref(VIPS_AREA(corners_array)); }
    return result;
}

int purevipsgen_sequential(VipsImage* in, VipsImage** out) {
    return vips_sequential(in, out, NULL);
}

int purevipsgen_sequential_with_options(VipsImage* in, VipsImage** out, gint tile_height) {
    VipsOperation *operation = vips_operation_new("sequential");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "tile_height", tile_height)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_sharpen(VipsImage* in, VipsImage** out) {
    return vips_sharpen(in, out, NULL);
}

int purevipsgen_sharpen_with_options(VipsImage* in, VipsImage** out, double sigma, double x1, double y2, double y3, double m1, double m2) {
    VipsOperation *operation = vips_operation_new("sharpen");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_double(operation, "sigma", sigma) ||
        purevipsgen_set_double(operation, "x1", x1) ||
        purevipsgen_set_double(operation, "y2", y2) ||
        purevipsgen_set_double(operation, "y3", y3) ||
        purevipsgen_set_double(operation, "m1", m1) ||
        purevipsgen_set_double(operation, "m2", m2)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_shrink(VipsImage* in, VipsImage** out, double hshrink, double vshrink) {
    return vips_shrink(in, out, hshrink, vshrink, NULL);
}

int purevipsgen_shrink_with_options(VipsImage* in, VipsImage** out, double hshrink, double vshrink, gboolean ceil) {
    VipsOperation *operation = vips_operation_new("shrink");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "hshrink", hshrink, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "vshrink", vshrink, NULL) ||
        purevipsgen_set_bool(operation, "ceil", ceil)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_shrinkh(VipsImage* in, VipsImage** out, gint hshrink) {
    return vips_shrinkh(in, out, hshrink, NULL);
}

int purevipsgen_shrinkh_with_options(VipsImage* in, VipsImage** out, gint hshrink, gboolean ceil) {
    VipsOperation *operation = vips_operation_new("shrinkh");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "hshrink", hshrink, NULL) ||
        purevipsgen_set_bool(operation, "ceil", ceil)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_shrinkv(VipsImage* in, VipsImage** out, gint vshrink) {
    return vips_shrinkv(in, out, vshrink, NULL);
}

int purevipsgen_shrinkv_with_options(VipsImage* in, VipsImage** out, gint vshrink, gboolean ceil) {
    VipsOperation *operation = vips_operation_new("shrinkv");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "vshrink", vshrink, NULL) ||
        purevipsgen_set_bool(operation, "ceil", ceil)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_sign(VipsImage* in, VipsImage** out) {
    return vips_sign(in, out, NULL);
}

int purevipsgen_similarity(VipsImage* in, VipsImage** out) {
    return vips_similarity(in, out, NULL);
}

int purevipsgen_similarity_with_options(VipsImage* in, VipsImage** out, double scale, double angle, VipsInterpolate* interpolate, double* background, int background_n, double odx, double ody, double idx, double idy) {
    VipsOperation *operation = vips_operation_new("similarity");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_double(operation, "scale", scale) ||
        purevipsgen_set_double(operation, "angle", angle) ||
        purevipsgen_set_interpolate(operation, "interpolate", interpolate) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_double(operation, "odx", odx) ||
        purevipsgen_set_double(operation, "ody", ody) ||
        purevipsgen_set_double(operation, "idx", idx) ||
        purevipsgen_set_double(operation, "idy", idy)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_sines(VipsImage** out, gint width, gint height) {
    return vips_sines(out, width, height, NULL);
}

int purevipsgen_sines_with_options(VipsImage** out, gint width, gint height, gboolean uchar, double hfreq, double vfreq) {
    VipsOperation *operation = vips_operation_new("sines");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar) ||
        purevipsgen_set_double(operation, "hfreq", hfreq) ||
        purevipsgen_set_double(operation, "vfreq", vfreq)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_smartcrop(VipsImage* input, VipsImage** out, gint width, gint height) {
    return vips_smartcrop(input, out, width, height, NULL);
}

int purevipsgen_smartcrop_with_options(VipsImage* input, VipsImage** out, gint width, gint height, VipsInteresting interesting, gboolean premultiplied, gint* attention_x, gint* attention_y) {
    VipsOperation *operation = vips_operation_new("smartcrop");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "input", input, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_int(operation, "interesting", interesting) ||
        purevipsgen_set_bool(operation, "premultiplied", premultiplied)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, "attention_x", attention_x, "attention_y", attention_y, NULL);
    return result;
}

int purevipsgen_sobel(VipsImage* in, VipsImage** out) {
    return vips_sobel(in, out, NULL);
}

int purevipsgen_spcor(VipsImage* in, VipsImage* ref, VipsImage** out) {
    return vips_spcor(in, ref, out, NULL);
}

int purevipsgen_spectrum(VipsImage* in, VipsImage** out) {
    return vips_spectrum(in, out, NULL);
}

int purevipsgen_stats(VipsImage* in, VipsImage** out) {
    return vips_stats(in, out, NULL);
}

int purevipsgen_stdif(VipsImage* in, VipsImage** out, gint width, gint height) {
    return vips_stdif(in, out, width, height, NULL);
}

int purevipsgen_stdif_with_options(VipsImage* in, VipsImage** out, gint width, gint height, double s0, double b, double m0, double a) {
    VipsOperation *operation = vips_operation_new("stdif");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_double(operation, "s0", s0) ||
        purevipsgen_set_double(operation, "b", b) ||
        purevipsgen_set_double(operation, "m0", m0) ||
        purevipsgen_set_double(operation, "a", a)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_subsample(VipsImage* input, VipsImage** out, gint xfac, gint yfac) {
    return vips_subsample(input, out, xfac, yfac, NULL);
}

int purevipsgen_subsample_with_options(VipsImage* input, VipsImage** out, gint xfac, gint yfac, gboolean point) {
    VipsOperation *operation = vips_operation_new("subsample");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "input", input, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "xfac", xfac, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "yfac", yfac, NULL) ||
        purevipsgen_set_bool(operation, "point", point)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_subtract(VipsImage* left, VipsImage* right, VipsImage** out) {
    return vips_subtract(left, right, out, NULL);
}

int purevipsgen_sum(VipsImage** in, VipsImage** out, int n) {
    return vips_sum(in, out, n, NULL);
}

int purevipsgen_svgload(const char* filename, VipsImage** out) {
    return vips_svgload(filename, out, NULL);
}

int purevipsgen_svgload_with_options(const char* filename, VipsImage** out, double dpi, double scale, gboolean unlimited, const char* stylesheet, gboolean high_bitdepth, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("svgload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_double(operation, "dpi", dpi) ||
        purevipsgen_set_double(operation, "scale", scale) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_string(operation, "stylesheet", stylesheet) ||
        purevipsgen_set_bool(operation, "high_bitdepth", high_bitdepth) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_svgload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_svgload_buffer(buf, len, out, NULL);
}

int purevipsgen_svgload_buffer_with_options(void* buf, size_t len, VipsImage** out, double dpi, double scale, gboolean unlimited, const char* stylesheet, gboolean high_bitdepth, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("svgload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_double(operation, "dpi", dpi) ||
        purevipsgen_set_double(operation, "scale", scale) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_string(operation, "stylesheet", stylesheet) ||
        purevipsgen_set_bool(operation, "high_bitdepth", high_bitdepth) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_svgload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_svgload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_svgload_source_with_options(VipsSourceCustom* source, VipsImage** out, double dpi, double scale, gboolean unlimited, const char* stylesheet, gboolean high_bitdepth, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("svgload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_double(operation, "dpi", dpi) ||
        purevipsgen_set_double(operation, "scale", scale) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_string(operation, "stylesheet", stylesheet) ||
        purevipsgen_set_bool(operation, "high_bitdepth", high_bitdepth) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_switch(VipsImage** tests, VipsImage** out, int n) {
    return vips_switch(tests, out, n, NULL);
}

int purevipsgen_system(const char* cmd_format) {
    return vips_system(cmd_format, NULL);
}

int purevipsgen_system_with_options(const char* cmd_format, VipsImage** in, int in_n, const char* out_format, const char* in_format) {
    VipsOperation *operation = vips_operation_new("system");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "cmd_format", cmd_format, NULL) ||
        purevipsgen_set_string(operation, "out_format", out_format) ||
        purevipsgen_set_string(operation, "in_format", in_format)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    return result;
}

int purevipsgen_text(VipsImage** out, const char* text) {
    return vips_text(out, text, NULL);
}

int purevipsgen_text_with_options(VipsImage** out, const char* text, const char* font, gint width, gint height, VipsAlign align, gboolean justify, gint dpi, gint spacing, const char* fontfile, gboolean rgba, VipsTextWrap wrap, gint* autofit_dpi) {
    VipsOperation *operation = vips_operation_new("text");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "text", text, NULL) ||
        purevipsgen_set_string(operation, "font", font) ||
        purevipsgen_set_int(operation, "width", width) ||
        purevipsgen_set_int(operation, "height", height) ||
        purevipsgen_set_int(operation, "align", align) ||
        purevipsgen_set_bool(operation, "justify", justify) ||
        purevipsgen_set_int(operation, "dpi", dpi) ||
        purevipsgen_set_int(operation, "spacing", spacing) ||
        purevipsgen_set_string(operation, "fontfile", fontfile) ||
        purevipsgen_set_bool(operation, "rgba", rgba) ||
        purevipsgen_set_int(operation, "wrap", wrap)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, "autofit_dpi", autofit_dpi, NULL);
    return result;
}

int purevipsgen_thumbnail(const char* filename, VipsImage** out, gint width) {
    return vips_thumbnail(filename, out, width, NULL);
}

int purevipsgen_thumbnail_with_options(const char* filename, VipsImage** out, gint width, gint height, VipsSize size, gboolean no_rotate, VipsInteresting crop, gboolean linear, const char* input_profile, const char* output_profile, VipsIntent intent, VipsFailOn fail_on) {
    VipsOperation *operation = vips_operation_new("thumbnail");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        purevipsgen_set_int(operation, "height", height) ||
        purevipsgen_set_int(operation, "size", size) ||
        purevipsgen_set_bool(operation, "no_rotate", no_rotate) ||
        purevipsgen_set_int(operation, "crop", crop) ||
        purevipsgen_set_bool(operation, "linear", linear) ||
        purevipsgen_set_string(operation, "input_profile", input_profile) ||
        purevipsgen_set_string(operation, "output_profile", output_profile) ||
        purevipsgen_set_int(operation, "intent", intent) ||
        purevipsgen_set_int(operation, "fail_on", fail_on)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_thumbnail_buffer(void* buf, size_t len, VipsImage** out, gint width) {
    return vips_thumbnail_buffer(buf, len, out, width, NULL);
}

int purevipsgen_thumbnail_buffer_with_options(void* buf, size_t len, VipsImage** out, gint width, const char* option_string, gint height, VipsSize size, gboolean no_rotate, VipsInteresting crop, gboolean linear, const char* input_profile, const char* output_profile, VipsIntent intent, VipsFailOn fail_on) {
    VipsOperation *operation = vips_operation_new("thumbnail_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        purevipsgen_set_string(operation, "option_string", option_string) ||
        purevipsgen_set_int(operation, "height", height) ||
        purevipsgen_set_int(operation, "size", size) ||
        purevipsgen_set_bool(operation, "no_rotate", no_rotate) ||
        purevipsgen_set_int(operation, "crop", crop) ||
        purevipsgen_set_bool(operation, "linear", linear) ||
        purevipsgen_set_string(operation, "input_profile", input_profile) ||
        purevipsgen_set_string(operation, "output_profile", output_profile) ||
        purevipsgen_set_int(operation, "intent", intent) ||
        purevipsgen_set_int(operation, "fail_on", fail_on)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_thumbnail_image(VipsImage* in, VipsImage** out, gint width) {
    return vips_thumbnail_image(in, out, width, NULL);
}

int purevipsgen_thumbnail_image_with_options(VipsImage* in, VipsImage** out, gint width, gint height, VipsSize size, gboolean no_rotate, VipsInteresting crop, gboolean linear, const char* input_profile, const char* output_profile, VipsIntent intent, VipsFailOn fail_on) {
    VipsOperation *operation = vips_operation_new("thumbnail_image");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        purevipsgen_set_int(operation, "height", height) ||
        purevipsgen_set_int(operation, "size", size) ||
        purevipsgen_set_bool(operation, "no_rotate", no_rotate) ||
        purevipsgen_set_int(operation, "crop", crop) ||
        purevipsgen_set_bool(operation, "linear", linear) ||
        purevipsgen_set_string(operation, "input_profile", input_profile) ||
        purevipsgen_set_string(operation, "output_profile", output_profile) ||
        purevipsgen_set_int(operation, "intent", intent) ||
        purevipsgen_set_int(operation, "fail_on", fail_on)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_thumbnail_source(VipsSourceCustom* source, VipsImage** out, gint width) {
    return vips_thumbnail_source((VipsSource*) source, out, width, NULL);
}

int purevipsgen_thumbnail_source_with_options(VipsSourceCustom* source, VipsImage** out, gint width, const char* option_string, gint height, VipsSize size, gboolean no_rotate, VipsInteresting crop, gboolean linear, const char* input_profile, const char* output_profile, VipsIntent intent, VipsFailOn fail_on) {
    VipsOperation *operation = vips_operation_new("thumbnail_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        purevipsgen_set_string(operation, "option_string", option_string) ||
        purevipsgen_set_int(operation, "height", height) ||
        purevipsgen_set_int(operation, "size", size) ||
        purevipsgen_set_bool(operation, "no_rotate", no_rotate) ||
        purevipsgen_set_int(operation, "crop", crop) ||
        purevipsgen_set_bool(operation, "linear", linear) ||
        purevipsgen_set_string(operation, "input_profile", input_profile) ||
        purevipsgen_set_string(operation, "output_profile", output_profile) ||
        purevipsgen_set_int(operation, "intent", intent) ||
        purevipsgen_set_int(operation, "fail_on", fail_on)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_tiffload(const char* filename, VipsImage** out) {
    return vips_tiffload(filename, out, NULL);
}

int purevipsgen_tiffload_with_options(const char* filename, VipsImage** out, gint page, gint n, gboolean autorotate, gint subifd, gboolean unlimited, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("tiffload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_bool(operation, "autorotate", autorotate) ||
        purevipsgen_set_int(operation, "subifd", subifd) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_tiffload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_tiffload_buffer(buf, len, out, NULL);
}

int purevipsgen_tiffload_buffer_with_options(void* buf, size_t len, VipsImage** out, gint page, gint n, gboolean autorotate, gint subifd, gboolean unlimited, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("tiffload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_bool(operation, "autorotate", autorotate) ||
        purevipsgen_set_int(operation, "subifd", subifd) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_tiffload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_tiffload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_tiffload_source_with_options(VipsSourceCustom* source, VipsImage** out, gint page, gint n, gboolean autorotate, gint subifd, gboolean unlimited, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("tiffload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_bool(operation, "autorotate", autorotate) ||
        purevipsgen_set_int(operation, "subifd", subifd) ||
        purevipsgen_set_bool(operation, "unlimited", unlimited) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_tiffsave(VipsImage* in, const char* filename) {
    return vips_tiffsave(in, filename, NULL);
}

int purevipsgen_tiffsave_with_options(VipsImage* in, const char* filename, VipsForeignTiffCompression compression, gint Q, VipsForeignTiffPredictor predictor, gboolean tile, gint tile_width, gint tile_height, gboolean pyramid, gboolean miniswhite, gint bitdepth, VipsForeignTiffResunit resunit, double xres, double yres, gboolean bigtiff, gboolean properties, VipsRegionShrink region_shrink, gint level, gboolean lossless, VipsForeignDzDepth depth, gboolean subifd, gboolean premultiply, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("tiffsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "compression", compression) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_int(operation, "predictor", predictor) ||
        purevipsgen_set_bool(operation, "tile", tile) ||
        purevipsgen_set_int(operation, "tile_width", tile_width) ||
        purevipsgen_set_int(operation, "tile_height", tile_height) ||
        purevipsgen_set_bool(operation, "pyramid", pyramid) ||
        purevipsgen_set_bool(operation, "miniswhite", miniswhite) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_int(operation, "resunit", resunit) ||
        purevipsgen_set_double(operation, "xres", xres) ||
        purevipsgen_set_double(operation, "yres", yres) ||
        purevipsgen_set_bool(operation, "bigtiff", bigtiff) ||
        purevipsgen_set_bool(operation, "properties", properties) ||
        purevipsgen_set_int(operation, "region_shrink", region_shrink) ||
        purevipsgen_set_int(operation, "level", level) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "depth", depth) ||
        purevipsgen_set_bool(operation, "subifd", subifd) ||
        purevipsgen_set_bool(operation, "premultiply", premultiply) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_tiffsave_buffer(VipsImage* in, void** buf, size_t* len) {
    return vips_tiffsave_buffer(in, buf, len, NULL);
}

int purevipsgen_tiffsave_buffer_with_options(VipsImage* in, void** buf, size_t* len, VipsForeignTiffCompression compression, gint Q, VipsForeignTiffPredictor predictor, gboolean tile, gint tile_width, gint tile_height, gboolean pyramid, gboolean miniswhite, gint bitdepth, VipsForeignTiffResunit resunit, double xres, double yres, gboolean bigtiff, gboolean properties, VipsRegionShrink region_shrink, gint level, gboolean lossless, VipsForeignDzDepth depth, gboolean subifd, gboolean premultiply, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("tiffsave_buffer");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "compression", compression) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_int(operation, "predictor", predictor) ||
        purevipsgen_set_bool(operation, "tile", tile) ||
        purevipsgen_set_int(operation, "tile_width", tile_width) ||
        purevipsgen_set_int(operation, "tile_height", tile_height) ||
        purevipsgen_set_bool(operation, "pyramid", pyramid) ||
        purevipsgen_set_bool(operation, "miniswhite", miniswhite) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_int(operation, "resunit", resunit) ||
        purevipsgen_set_double(operation, "xres", xres) ||
        purevipsgen_set_double(operation, "yres", yres) ||
        purevipsgen_set_bool(operation, "bigtiff", bigtiff) ||
        purevipsgen_set_bool(operation, "properties", properties) ||
        purevipsgen_set_int(operation, "region_shrink", region_shrink) ||
        purevipsgen_set_int(operation, "level", level) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "depth", depth) ||
        purevipsgen_set_bool(operation, "subifd", subifd) ||
        purevipsgen_set_bool(operation, "premultiply", premultiply) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_save_buffer(operation, buf, len);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_tiffsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_tiffsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_tiffsave_target_with_options(VipsImage* in, VipsTargetCustom* target, VipsForeignTiffCompression compression, gint Q, VipsForeignTiffPredictor predictor, gboolean tile, gint tile_width, gint tile_height, gboolean pyramid, gboolean miniswhite, gint bitdepth, VipsForeignTiffResunit resunit, double xres, double yres, gboolean bigtiff, gboolean properties, VipsRegionShrink region_shrink, gint level, gboolean lossless, VipsForeignDzDepth depth, gboolean subifd, gboolean premultiply, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("tiffsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_int(operation, "compression", compression) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_int(operation, "predictor", predictor) ||
        purevipsgen_set_bool(operation, "tile", tile) ||
        purevipsgen_set_int(operation, "tile_width", tile_width) ||
        purevipsgen_set_int(operation, "tile_height", tile_height) ||
        purevipsgen_set_bool(operation, "pyramid", pyramid) ||
        purevipsgen_set_bool(operation, "miniswhite", miniswhite) ||
        purevipsgen_set_int(operation, "bitdepth", bitdepth) ||
        purevipsgen_set_int(operation, "resunit", resunit) ||
        purevipsgen_set_double(operation, "xres", xres) ||
        purevipsgen_set_double(operation, "yres", yres) ||
        purevipsgen_set_bool(operation, "bigtiff", bigtiff) ||
        purevipsgen_set_bool(operation, "properties", properties) ||
        purevipsgen_set_int(operation, "region_shrink", region_shrink) ||
        purevipsgen_set_int(operation, "level", level) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "depth", depth) ||
        purevipsgen_set_bool(operation, "subifd", subifd) ||
        purevipsgen_set_bool(operation, "premultiply", premultiply) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_tilecache(VipsImage* in, VipsImage** out) {
    return vips_tilecache(in, out, NULL);
}

int purevipsgen_tilecache_with_options(VipsImage* in, VipsImage** out, gint tile_width, gint tile_height, gint max_tiles, VipsAccess access, gboolean threaded, gboolean persistent) {
    VipsOperation *operation = vips_operation_new("tilecache");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "tile_width", tile_width) ||
        purevipsgen_set_int(operation, "tile_height", tile_height) ||
        purevipsgen_set_int(operation, "max_tiles", max_tiles) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_bool(operation, "threaded", threaded) ||
        purevipsgen_set_bool(operation, "persistent", persistent)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_tonelut(VipsImage** out) {
    return vips_tonelut(out, NULL);
}

int purevipsgen_tonelut_with_options(VipsImage** out, gint in_max, gint out_max, double Lb, double Lw, double Ps, double Pm, double Ph, double S, double M, double H) {
    VipsOperation *operation = vips_operation_new("tonelut");
    if (!operation) return 1;
    if (
        purevipsgen_set_int(operation, "in_max", in_max) ||
        purevipsgen_set_int(operation, "out_max", out_max) ||
        purevipsgen_set_double(operation, "Lb", Lb) ||
        purevipsgen_set_double(operation, "Lw", Lw) ||
        purevipsgen_set_double(operation, "Ps", Ps) ||
        purevipsgen_set_double(operation, "Pm", Pm) ||
        purevipsgen_set_double(operation, "Ph", Ph) ||
        purevipsgen_set_double(operation, "S", S) ||
        purevipsgen_set_double(operation, "M", M) ||
        purevipsgen_set_double(operation, "H", H)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_transpose3d(VipsImage* in, VipsImage** out) {
    return vips_transpose3d(in, out, NULL);
}

int purevipsgen_transpose3d_with_options(VipsImage* in, VipsImage** out, gint page_height) {
    VipsOperation *operation = vips_operation_new("transpose3d");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "page_height", page_height)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_unpremultiply(VipsImage* in, VipsImage** out) {
    return vips_unpremultiply(in, out, NULL);
}

int purevipsgen_unpremultiply_with_options(VipsImage* in, VipsImage** out, double max_alpha, gint alpha_band) {
    VipsOperation *operation = vips_operation_new("unpremultiply");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_double(operation, "max_alpha", max_alpha) ||
        purevipsgen_set_int(operation, "alpha_band", alpha_band)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_vipsload(const char* filename, VipsImage** out) {
    return vips_vipsload(filename, out, NULL);
}

int purevipsgen_vipsload_with_options(const char* filename, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("vipsload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_vipsload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_vipsload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_vipsload_source_with_options(VipsSourceCustom* source, VipsImage** out, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("vipsload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_vipssave(VipsImage* in, const char* filename) {
    return vips_vipssave(in, filename, NULL);
}

int purevipsgen_vipssave_with_options(VipsImage* in, const char* filename, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("vipssave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_vipssave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_vipssave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_vipssave_target_with_options(VipsImage* in, VipsTargetCustom* target, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("vipssave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_webpload(const char* filename, VipsImage** out) {
    return vips_webpload(filename, out, NULL);
}

int purevipsgen_webpload_with_options(const char* filename, VipsImage** out, gint page, gint n, double scale, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("webpload");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_double(operation, "scale", scale) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_webpload_buffer(void* buf, size_t len, VipsImage** out) {
    return vips_webpload_buffer(buf, len, out, NULL);
}

int purevipsgen_webpload_buffer_with_options(void* buf, size_t len, VipsImage** out, gint page, gint n, double scale, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("webpload_buffer");
    if (!operation) return 1;
    VipsBlob *blob = vips_blob_new(NULL, buf, len);
    if (!blob) { g_object_unref(operation); return 1; }
    if (
        vips_object_set(VIPS_OBJECT(operation), "buffer", blob, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_double(operation, "scale", scale) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        vips_area_unref((VipsArea *)blob);
        g_object_unref(operation);
        return 1;
    }
    vips_area_unref((VipsArea *)blob);
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_webpload_source(VipsSourceCustom* source, VipsImage** out) {
    return vips_webpload_source((VipsSource*) source, out, NULL);
}

int purevipsgen_webpload_source_with_options(VipsSourceCustom* source, VipsImage** out, gint page, gint n, double scale, gboolean memory, VipsAccess access, VipsFailOn fail_on, gboolean revalidate) {
    VipsOperation *operation = vips_operation_new("webpload_source");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "source", (VipsSource*)source, NULL) ||
        purevipsgen_set_int(operation, "page", page) ||
        purevipsgen_set_int(operation, "n", n) ||
        purevipsgen_set_double(operation, "scale", scale) ||
        purevipsgen_set_bool(operation, "memory", memory) ||
        purevipsgen_set_int(operation, "access", access) ||
        purevipsgen_set_int(operation, "fail_on", fail_on) ||
        purevipsgen_set_bool(operation, "revalidate", revalidate)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_webpsave(VipsImage* in, const char* filename) {
    return vips_webpsave(in, filename, NULL);
}

int purevipsgen_webpsave_with_options(VipsImage* in, const char* filename, gint Q, gboolean lossless, VipsForeignWebpPreset preset, gboolean smart_subsample, gboolean near_lossless, gint alpha_q, gboolean min_size, gint kmin, gint kmax, gint effort, gint target_size, gboolean mixed, gboolean smart_deblock, gint passes, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("webpsave");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "filename", filename, NULL) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "preset", preset) ||
        purevipsgen_set_bool(operation, "smart_subsample", smart_subsample) ||
        purevipsgen_set_bool(operation, "near_lossless", near_lossless) ||
        purevipsgen_set_int(operation, "alpha_q", alpha_q) ||
        purevipsgen_set_bool(operation, "min_size", min_size) ||
        purevipsgen_set_int(operation, "kmin", kmin) ||
        purevipsgen_set_int(operation, "kmax", kmax) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_int(operation, "target_size", target_size) ||
        purevipsgen_set_bool(operation, "mixed", mixed) ||
        purevipsgen_set_bool(operation, "smart_deblock", smart_deblock) ||
        purevipsgen_set_int(operation, "passes", passes) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_webpsave_buffer(VipsImage* in, void** buf, size_t* len) {
    return vips_webpsave_buffer(in, buf, len, NULL);
}

int purevipsgen_webpsave_buffer_with_options(VipsImage* in, void** buf, size_t* len, gint Q, gboolean lossless, VipsForeignWebpPreset preset, gboolean smart_subsample, gboolean near_lossless, gint alpha_q, gboolean min_size, gint kmin, gint kmax, gint effort, gint target_size, gboolean mixed, gboolean smart_deblock, gint passes, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("webpsave_buffer");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "preset", preset) ||
        purevipsgen_set_bool(operation, "smart_subsample", smart_subsample) ||
        purevipsgen_set_bool(operation, "near_lossless", near_lossless) ||
        purevipsgen_set_int(operation, "alpha_q", alpha_q) ||
        purevipsgen_set_bool(operation, "min_size", min_size) ||
        purevipsgen_set_int(operation, "kmin", kmin) ||
        purevipsgen_set_int(operation, "kmax", kmax) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_int(operation, "target_size", target_size) ||
        purevipsgen_set_bool(operation, "mixed", mixed) ||
        purevipsgen_set_bool(operation, "smart_deblock", smart_deblock) ||
        purevipsgen_set_int(operation, "passes", passes) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_save_buffer(operation, buf, len);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_webpsave_target(VipsImage* in, VipsTargetCustom* target) {
    return vips_webpsave_target(in, (VipsTarget*) target, NULL);
}

int purevipsgen_webpsave_target_with_options(VipsImage* in, VipsTargetCustom* target, gint Q, gboolean lossless, VipsForeignWebpPreset preset, gboolean smart_subsample, gboolean near_lossless, gint alpha_q, gboolean min_size, gint kmin, gint kmax, gint effort, gint target_size, gboolean mixed, gboolean smart_deblock, gint passes, VipsForeignKeep keep, double* background, int background_n, gint page_height, const char* profile) {
    VipsOperation *operation = vips_operation_new("webpsave_target");
    if (!operation) return 1;
    VipsArrayDouble *background_array = NULL;
    if (background != NULL && background_n > 0) { background_array = vips_array_double_new(background, background_n); }
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "target", (VipsTarget*)target, NULL) ||
        purevipsgen_set_int(operation, "Q", Q) ||
        purevipsgen_set_bool(operation, "lossless", lossless) ||
        purevipsgen_set_int(operation, "preset", preset) ||
        purevipsgen_set_bool(operation, "smart_subsample", smart_subsample) ||
        purevipsgen_set_bool(operation, "near_lossless", near_lossless) ||
        purevipsgen_set_int(operation, "alpha_q", alpha_q) ||
        purevipsgen_set_bool(operation, "min_size", min_size) ||
        purevipsgen_set_int(operation, "kmin", kmin) ||
        purevipsgen_set_int(operation, "kmax", kmax) ||
        purevipsgen_set_int(operation, "effort", effort) ||
        purevipsgen_set_int(operation, "target_size", target_size) ||
        purevipsgen_set_bool(operation, "mixed", mixed) ||
        purevipsgen_set_bool(operation, "smart_deblock", smart_deblock) ||
        purevipsgen_set_int(operation, "passes", passes) ||
        purevipsgen_set_keep(operation, keep) ||
        purevipsgen_set_array_double(operation, "background", background_array) ||
        purevipsgen_set_int(operation, "page_height", page_height) ||
        purevipsgen_set_string(operation, "profile", profile)
    ) {
        g_object_unref(operation);
        if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, NULL);
    if (background_array != NULL) { vips_area_unref(VIPS_AREA(background_array)); }
    return result;
}

int purevipsgen_worley(VipsImage** out, gint width, gint height) {
    return vips_worley(out, width, height, NULL);
}

int purevipsgen_worley_with_options(VipsImage** out, gint width, gint height, gint cell_size, gint seed) {
    VipsOperation *operation = vips_operation_new("worley");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_int(operation, "cell_size", cell_size) ||
        purevipsgen_set_int(operation, "seed", seed)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_wrap(VipsImage* in, VipsImage** out) {
    return vips_wrap(in, out, NULL);
}

int purevipsgen_wrap_with_options(VipsImage* in, VipsImage** out, gint x, gint y) {
    VipsOperation *operation = vips_operation_new("wrap");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "in", in, NULL) ||
        purevipsgen_set_int(operation, "x", x) ||
        purevipsgen_set_int(operation, "y", y)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_xyz(VipsImage** out, gint width, gint height) {
    return vips_xyz(out, width, height, NULL);
}

int purevipsgen_xyz_with_options(VipsImage** out, gint width, gint height, gint csize, gint dsize, gint esize) {
    VipsOperation *operation = vips_operation_new("xyz");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_int(operation, "csize", csize) ||
        purevipsgen_set_int(operation, "dsize", dsize) ||
        purevipsgen_set_int(operation, "esize", esize)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_zone(VipsImage** out, gint width, gint height) {
    return vips_zone(out, width, height, NULL);
}

int purevipsgen_zone_with_options(VipsImage** out, gint width, gint height, gboolean uchar) {
    VipsOperation *operation = vips_operation_new("zone");
    if (!operation) return 1;
    if (
        vips_object_set(VIPS_OBJECT(operation), "width", width, NULL) ||
        vips_object_set(VIPS_OBJECT(operation), "height", height, NULL) ||
        purevipsgen_set_bool(operation, "uchar", uchar)
    ) {
        g_object_unref(operation);
        return 1;
    }
    int result = purevipsgen_operation_execute(operation, "out", out, NULL);
    return result;
}

int purevipsgen_zoom(VipsImage* input, VipsImage** out, gint xfac, gint yfac) {
    return vips_zoom(input, out, xfac, yfac, NULL);
}


// Custom operations

int purevipsgen_image_new_from_source(VipsSourceCustom *source, VipsImage **out) {
  *out = vips_image_new_from_source((VipsSource*) source, "", NULL);
  if (!*out) return 1;
  return 0;
}

int purevipsgen_image_new_from_source_with_option(VipsSourceCustom *source, VipsImage **out, const char *option_string) {
  *out = vips_image_new_from_source((VipsSource*) source, option_string, NULL);
  if (!*out) return 1;
  return 0;
}

int purevipsgen_image_new_from_file(const char *name, VipsImage **out) {
  *out = vips_image_new_from_file(name, NULL);
  if (!*out) return 1;
  return 0;
}

int purevipsgen_image_new_from_file_with_option(const char *name, VipsImage **out, const char *option_string) {
  // Use vips_foreign_find_load on the raw filename (no options appended) to avoid
  // the libvips string parser mishandling filenames that start with ',' or other
  // tokenizer-special characters. Then set options via GObject properties directly.
  const char *loader = vips_foreign_find_load(name);
  if (!loader) return 1;

  VipsOperation *operation = vips_operation_new(loader);
  if (!operation) return 1;

  if (vips_object_set(VIPS_OBJECT(operation), "filename", name, NULL)) {
    g_object_unref(operation);
    return 1;
  }

  if (option_string && strlen(option_string) > 0) {
    if (vips_object_set_from_string(VIPS_OBJECT(operation), option_string)) {
      g_object_unref(operation);
      return 1;
    }
  }

  if (vips_cache_operation_buildp(&operation)) {
    vips_object_unref_outputs(VIPS_OBJECT(operation));
    g_object_unref(operation);
    return 1;
  }
  g_object_get(VIPS_OBJECT(operation), "out", out, NULL);
  vips_object_unref_outputs(VIPS_OBJECT(operation));
  g_object_unref(operation);
  return 0;
}

int purevipsgen_image_new_from_buffer(const void *buf, size_t len, VipsImage **out) {
  *out = vips_image_new_from_buffer(buf, len, "", NULL);
  if (!*out) return 1;
  return 0;
}

int purevipsgen_image_new_from_memory(const void *buf, size_t len, int width, int height, int bands, VipsImage **out) {
  *out = vips_image_new_from_memory(buf, len, width, height, bands, VIPS_FORMAT_UCHAR);
  if (!*out) return 1;
  return 0;
}

int purevipsgen_image_write_to_memory(VipsImage *in, void **buf, size_t *len) {
  *buf = vips_image_write_to_memory(in, len);
  if (!*buf) return 1;
  return 0;
}

int purevipsgen_image_new_from_buffer_with_option(const void *buf, size_t len, VipsImage **out, const char *option_string) {
  *out = vips_image_new_from_buffer(buf, len, option_string, NULL);
  if (!*out) return 1;
  return 0;
}

void purevipsgen_clear_image(VipsImage **image) {
  // https://developer.gnome.org/gobject/stable/gobject-The-Base-Object-Type.html#g-clear-object
  if (G_IS_OBJECT(*image)) g_clear_object(image);
}

int purevipsgen_remove_exif(VipsImage *in, VipsImage **out) {
  static double default_resolution = 72.0 / 25.4;

  if (vips_copy(
    in, out,
    "xres", default_resolution,
    "yres", default_resolution,
    NULL
  )) return 1;

  gchar **fields = vips_image_get_fields(in);

  for (int i = 0; fields[i] != NULL; i++) {
    gchar *name = fields[i];
    if (strcmp(name, VIPS_META_ICC_NAME) == 0) continue;
    if (strcmp(name, VIPS_META_ORIENTATION) == 0) continue;
    if (strcmp(name, VIPS_META_N_PAGES) == 0) continue;
    if (strcmp(name, VIPS_META_PAGE_HEIGHT) == 0) continue;
    if (strcmp(name, "delay") == 0) continue;
    if (strcmp(name, "loop") == 0) continue;
    if (strcmp(name, "palette-bit-depth") == 0) continue;
    vips_image_remove(*out, name);
  }
  g_strfreev(fields);
  return 0;
}

int purevipsgen_embed_multi_page(VipsImage *in, VipsImage **out, int left, int top, int width,
                         int height, int extend) {
  VipsObject *base = VIPS_OBJECT(vips_image_new());
  int page_height = vips_image_get_page_height(in);
  int in_width = in->Xsize;
  int n_pages = in->Ysize / page_height;

  VipsImage **page = (VipsImage **) vips_object_local_array(base, n_pages);
  VipsImage **embedded_page = (VipsImage **) vips_object_local_array(base, n_pages);
  VipsImage **copy = (VipsImage **) vips_object_local_array(base, 1);

  // split image into cropped frames
  for (int i = 0; i < n_pages; i++) {
    if (
      vips_extract_area(in, &page[i], 0, page_height * i, in_width, page_height, NULL) ||
      vips_embed(page[i], &embedded_page[i], left, top, width, height, "extend", extend, NULL)
    ) {
      g_object_unref(base);
      return -1;
    }
  }
  // reassemble frames and set page height
  // copy before modifying metadata
  if(
    vips_arrayjoin(embedded_page, &copy[0], n_pages, "across", 1, NULL) ||
    vips_copy(copy[0], out, NULL)
  ) {
    g_object_unref(base);
    return -1;
  }
  vips_image_set_int(*out, VIPS_META_PAGE_HEIGHT, height);
  g_object_unref(base);
  return 0;
}

int purevipsgen_embed_multi_page_background(VipsImage *in, VipsImage **out, int left, int top, int width,
                                   int height, double r, double g, double b, double a) {
  double background[3] = {r, g, b};
  double backgroundRGBA[4] = {r, g, b, a};

  VipsArrayDouble *vipsBackground;

  if (in->Bands <= 3) {
    vipsBackground = vips_array_double_new(background, 3);
  } else {
    vipsBackground = vips_array_double_new(backgroundRGBA, 4);
  }
  VipsObject *base = VIPS_OBJECT(vips_image_new());
  int page_height = vips_image_get_page_height(in);
  int in_width = in->Xsize;
  int n_pages = in->Ysize / page_height;

  VipsImage **page = (VipsImage **) vips_object_local_array(base, n_pages);
  VipsImage **embedded_page = (VipsImage **) vips_object_local_array(base, n_pages);
  VipsImage **copy = (VipsImage **) vips_object_local_array(base, 1);

  // split image into cropped frames
  for (int i = 0; i < n_pages; i++) {
    if (
      vips_extract_area(in, &page[i], 0, page_height * i, in_width, page_height, NULL) ||
      vips_embed(page[i], &embedded_page[i], left, top, width, height,
          "extend", VIPS_EXTEND_BACKGROUND, "background", vipsBackground, NULL)
    ) {
      vips_area_unref(VIPS_AREA(vipsBackground));
      g_object_unref(base);
      return -1;
    }
  }
  // reassemble frames and set page height
  // copy before modifying metadata
  if(
    vips_arrayjoin(embedded_page, &copy[0], n_pages, "across", 1, NULL) ||
    vips_copy(copy[0], out, NULL)
  ) {
    vips_area_unref(VIPS_AREA(vipsBackground));
    g_object_unref(base);
    return -1;
  }
  vips_image_set_int(*out, VIPS_META_PAGE_HEIGHT, height);
  vips_area_unref(VIPS_AREA(vipsBackground));
  g_object_unref(base);
  return 0;
}

int purevipsgen_extract_area_multi_page(VipsImage *in, VipsImage **out, int left, int top, int width, int height) {
  VipsObject *base = VIPS_OBJECT(vips_image_new());
  int page_height = vips_image_get_page_height(in);
  int n_pages = in->Ysize / page_height;

  VipsImage **page = (VipsImage **) vips_object_local_array(base, n_pages);
  VipsImage **copy = (VipsImage **) vips_object_local_array(base, 1);

  // split image into cropped frames
  for (int i = 0; i < n_pages; i++) {
    if(vips_extract_area(in, &page[i], left, page_height * i + top, width, height, NULL)) {
      g_object_unref(base);
      return -1;
    }
  }
  // reassemble frames and set page height
  // copy before modifying metadata
  if(
    vips_arrayjoin(page, &copy[0], n_pages, "across", 1, NULL) ||
    vips_copy(copy[0], out, NULL)
  ) {
    g_object_unref(base);
    return -1;
  }
  vips_image_set_int(*out, VIPS_META_PAGE_HEIGHT, height);
  g_object_unref(base);
  return 0;
}

int purevipsgen_rot_multi_page(VipsImage *in, VipsImage **out, VipsAngle angle) {
  VipsObject *base = VIPS_OBJECT(vips_image_new());
  int page_height = vips_image_get_page_height(in);
  int in_width = in->Xsize;
  int n_pages = in->Ysize / page_height;

  VipsImage **page = (VipsImage **) vips_object_local_array(base, n_pages);
  VipsImage **rotated_page = (VipsImage **) vips_object_local_array(base, n_pages);
  VipsImage **copy = (VipsImage **) vips_object_local_array(base, 1);

  // split image into cropped frames
  for (int i = 0; i < n_pages; i++) {
    if (
      vips_extract_area(in, &page[i], 0, page_height * i, in_width, page_height, NULL) ||
      vips_rot(page[i], &rotated_page[i], angle, NULL)
    ) {
      g_object_unref(base);
      return -1;
    }
  }
  // reassemble frames and set page height if rotate 90 or 270
  // copy before modifying metadata
  if(
    vips_arrayjoin(rotated_page, &copy[0], n_pages, "across", 1, NULL) ||
    vips_copy(copy[0], out, NULL)
  ) {
    g_object_unref(base);
    return -1;
  }
  if (angle == VIPS_ANGLE_D90 || angle == VIPS_ANGLE_D270) {
    vips_image_set_int(*out, VIPS_META_PAGE_HEIGHT, in_width);
  }
  g_object_unref(base);
  return 0;
}
